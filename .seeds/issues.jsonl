{"id":"trellis-7cb8","title":"Implement trellis MVP — agentic-readiness audit & sync per SPEC.md","status":"open","type":"feature","priority":1,"createdAt":"2026-06-06T18:39:20.629Z","updatedAt":"2026-06-11T04:15:07.223Z","description":"Parent seed for the trellis MVP build-out. SPEC.md (repo root) is the design record: a mostly-deterministic, partly-agentic audit tool that scores repos 0-100% against a versioned 90-criterion rubric (9 categories, repo-scope + app-scope), maps to maturity Levels 1-5 with a coverage-aware clamp, detects canonical-config drift against a bundled standards/ set, and tracks fleet history in central SQLite. Deterministic detectors for TypeScript/Swift/Python; 4 bounded Pi-RPC investigation areas feed a deterministic grader. The plan also carries the os-eco repo standards: warren-stack scaffold (Bun + TS strict + Biome + zod + commander + pino), L5 toolkit ratchets + check:all, .github governance + CI/publish workflows, .claude/commands, AGENTS.md/CLAUDE.md/RUNBOOK and the rest of the standard root files.","plan_id":"pl-974b"}
{"id":"trellis-b636","title":"Scaffold the repo: Bun + TS-strict skeleton, os-eco baseline configs, package.json (@os-eco/trellis-cli, bin trellis), src/ tree per SPEC §4","status":"closed","type":"task","priority":1,"plan_step_index":0,"description":"Scaffold the trellis repo as a standard os-eco Bun + TypeScript sub-repo. DELIVERABLES (declarative end state): package.json exists with name @os-eco/trellis-cli, version 0.0.1, type module, bin { \"trellis\": \"./src/cli/main.ts\" }, engines.bun >= 1.1.0, author/license/repository fields matching the warren/seeds pattern; devDependencies include @biomejs/biome, @types/bun, typescript, knip; dependencies include commander, zod, pino, js-yaml (+ @types/js-yaml). src/index.ts exports `export const VERSION = \"0.0.1\"` (kept in lockstep with package.json — the release workflow asserts this). Baseline configs are copied from templates/l5-toolkit/configs/ and adapted: biome.json (kebab-case filenaming, complexity caps, tab indent / 100-col), tsconfig.base.json + tsconfig.json extending it (strict, noUncheckedIndexedAccess, no any), bunfig.toml, knip.json, .jscpd.json. .gitignore seeded from templates/l5-toolkit/env/.gitignore.template; .env.example present (investigation env keys: ANTHROPIC_API_KEY etc. documented, values blank); LICENSE is MIT. The src/ directory tree exists per SPEC §4 (cli/, client/, rubric/, discovery/, detectors/{common,lang/typescript,lang/swift,lang/python,oseco}/, investigation/provider/pi/, scoring/, standards/canonical/, fleet/, store/migrations/, report/) with placeholder index modules so typecheck passes. targets.yaml.example exists per SPEC §6.5. Package scripts: test, lint (biome check --error-on-warnings .), lint:fix, typecheck (tsc --noEmit). ACCEPTANCE: bun install && bun test && bun run lint && bun run typecheck all exit 0; `bun run src/cli/main.ts --help` prints a stub. Conventions: .ts import extensions, tests <name>.test.ts beside the unit. REFERENCES: SPEC §4, §13; warren/package.json; templates/l5-toolkit/configs/.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T18:52:35.063Z","labels":["standards","scaffold"],"plan_id":"pl-974b","blocks":["trellis-03a9","trellis-4ec4","trellis-2304","trellis-dd9c","trellis-7cb8"],"closedAt":"2026-06-06T18:52:35.063Z"}
{"id":"trellis-03a9","title":"Author governance & agent docs: README, AGENTS.md, CLAUDE.md, CHANGELOG, CODEOWNERS, CONTRIBUTING, SECURITY, RUNBOOK, docs/architecture.mmd, .claude/commands (release, pr-reviews, issue-reviews, prioritize), .mulch init","status":"closed","type":"task","priority":1,"plan_step_index":1,"description":"Author the standard os-eco governance and agent-instruction surface for trellis. DELIVERABLES: README.md (what trellis is, install, the six CLI commands from SPEC §12, targets.yaml example, ecosystem positioning); AGENTS.md populated from templates/l5-toolkit/docs/AGENTS.md.template with real commands (bun test / lint / typecheck / check:all), conventions (kebab-case, tab/100-col, .ts extensions, golden fixtures under __golden__/, debt-marker tracker prefixes trellis-XXXX/mx-XXXX/#NNN/URL), and agent workflow; CLAUDE.md mirroring the other sub-repos (tool-specific conventions, architecture summary, commands, sd/ml/cn session bootstrap); CHANGELOG.md in keep-a-changelog format with an [Unreleased] section; CODEOWNERS (* @jayminwest); CONTRIBUTING.md and SECURITY.md adapted from warren/seeds; RUNBOOK.md from the l5-toolkit template (release / triage / rollback procedures); docs/architecture.mmd mermaid diagram of the SPEC §4 module graph (rubric→detectors/investigation→scoring→report, store, fleet, standards). .claude/commands/ contains release.md (patch-default bump, package.json + src/index.ts VERSION sync, CHANGELOG roll, update CLAUDE.md/README, commit — mirror warren/.claude/commands/release.md), pr-reviews.md, issue-reviews.md, prioritize.md (mirror seeds/canopy). Run `ml init`-equivalent: .mulch/ initialized with a trellis domain. ACCEPTANCE: every file above exists and is non-trivial (AGENTS.md > 100 chars of real instruction); all commands named in AGENTS.md actually exist in package.json. REFERENCES: warren and seeds repo roots; templates/l5-toolkit/docs/; SPEC §13.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:04:41.995Z","labels":["standards","docs"],"plan_id":"pl-974b","blocks":["trellis-4ec4","trellis-3447","trellis-7cb8"],"closedAt":"2026-06-06T19:04:41.995Z"}
{"id":"trellis-4ec4","title":"Port the L5 quality toolkit: ratchet scripts + baselined budgets, validate-agents-md, reporters, pre-commit hook, check:* family wired into a green check:all","status":"closed","type":"task","priority":1,"plan_step_index":2,"description":"Port the L5 quality toolkit so trellis enforces the same ratchets it audits for. DELIVERABLES: scripts/check-file-sizes.ts, check-debt-markers.ts, check-coverage.ts, validate-agents-md.ts, report-test-timing.ts, report-quality-metrics.ts copied from templates/l5-toolkit/scripts/ and adapted (debt-marker tracker prefixes: trellis-, mx-, #, http); scripts/hooks/pre-commit (executable) installed via a `prepare` script (`[ -e .git ] && git config core.hooksPath scripts/hooks || true`); budget JSONs co-located with their scripts and BASELINED per l5-toolkit README §(b): file-size budget = ceil(observed_max/50)*50+50, coverage floor = floor(observed), debt markers = 0 with all existing markers pinned to a tracker. Each ported script keeps its co-located *.test.ts suite, adapted to trellis paths. package.json gains check:size, check:debt, check:dups (jscpd), check:deps (knip --dependencies), check:agents, check:coverage, test:ci (junit to test-results/junit.xml), test:coverage, and a check:all aggregator chaining coverage→lint→typecheck→agents→size→debt→dups→deps. ACCEPTANCE: bun run check:all exits 0; each ratchet script run standalone exits 0; bun run prepare installs the hooks path; budgets reflect truth on disk (never artificially loose). REFERENCES: templates/l5-toolkit/README.md §(b)+(d), templates/l5-toolkit/scripts/ + budgets/, warren/package.json scripts block.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:17:54.553Z","labels":["standards","quality"],"plan_id":"pl-974b","blocks":["trellis-7baf","trellis-7cb8"],"closedAt":"2026-06-06T19:17:54.553Z"}
{"id":"trellis-7baf","title":"Add .github governance + workflows: dependabot (cooldown), ISSUE_TEMPLATEs, PR template, labels.yml, ci.yml (check:all parity), sync-labels.yml, publish.yml (version-gated npm + provenance), auto-merge.yml","status":"closed","type":"task","priority":1,"plan_step_index":3,"description":"Add the standard os-eco .github governance and workflow set. DELIVERABLES: .github/dependabot.yml with the cooldown block (min_release_age evidence); .github/ISSUE_TEMPLATE/{bug_report.yml,feature_request.yml,config.yml}; .github/pull_request_template.md; .github/labels.yml + workflows/sync-labels.yml; workflows/ci.yml that runs on push/PR with oven-sh/setup-bun and executes bun install, bun run check:all (verbatim — CI parity with the local gate), bun run test:ci, the two report-* step summaries, and uploads junit.xml + coverage/lcov.info artifacts; workflows/publish.yml mirroring seeds/canopy: on push to main, run lint+typecheck+test, compare package.json version against npm view @os-eco/trellis-cli version, verify package.json ↔ src/index.ts VERSION sync, and npm publish --access public --provenance only when the version changed (NPM_TOKEN secret); workflows/auto-merge.yml mirroring seeds. ACCEPTANCE: all YAML parses (actionlint or yq clean); ci.yml invokes check:all exactly as defined in package.json (no drift between CI and local gates); publish.yml references @os-eco/trellis-cli. REFERENCES: seeds/.github/workflows/{ci,publish,sync-labels,auto-merge}.yml, templates/l5-toolkit/github/.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:22:09.291Z","labels":["standards","ci"],"plan_id":"pl-974b","blocks":["trellis-3447","trellis-7cb8"],"closedAt":"2026-06-06T19:22:09.291Z"}
{"id":"trellis-2304","title":"Rubric schema + loader: zod schemas for criterion/category records, RUBRIC_VERSION + comparability policy, load-time invariants (SPEC §6.1)","status":"closed","type":"feature","priority":1,"plan_step_index":4,"description":"Implement the rubric schema and loader — the WHAT layer that never names a tool. DELIVERABLES: src/rubric/schema.ts defines zod schemas for category records ({ id: snake_case, title, description }) and criterion records ({ id, category, scope: repo|app, level: 1-5, skippable: boolean, discoveryVia: deterministic|agent, investigation: documentation|agent-config|setup-runnability|test-layout|null, gate: boolean (reserved), weight: positive number default 1 (reserved) }) per SPEC §6.1. src/rubric/version.ts exports RUBRIC_VERSION = \"0.2.0\" plus a comment block recording the semver-by-comparability policy (major = moves scores for unchanged code; minor = additive; patch = wording; pre-1.0 comparability changes ride minor) per SPEC §3.5. A loader function parses categories.yaml / repo-scope.yaml / app-scope.yaml and enforces load-time invariants: investigation non-null IFF discoveryVia=agent; exactly one gate:true per category; weight > 0; every entry's scope matches its source file; category references resolve; ids unique. Violations throw with the offending id and file. DELIVERABLE TESTS: schema round-trip, each invariant violated in a fixture and rejected with a precise error. ACCEPTANCE: bun test green; loader exported from the rubric core module (no CLI yet). REFERENCES: SPEC §3.1-3.5, §6.1.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:27:30.252Z","labels":["rubric"],"plan_id":"pl-974b","blocks":["trellis-db3d","trellis-92f6","trellis-7cb8"],"closedAt":"2026-06-06T19:27:30.252Z"}
{"id":"trellis-db3d","title":"Author the rubric data: categories.yaml (9), repo-scope.yaml (44), app-scope.yaml (46) — the full §5 catalog with levels, gates, skippable flags, discoveryVia/investigation tags","status":"closed","type":"feature","priority":1,"plan_step_index":5,"description":"Author the full rubric data catalog — 90 criteria across 9 categories, exactly as tabulated in SPEC §5. DELIVERABLES: src/rubric/categories.yaml with the 9 categories (documentation, code_quality, testing, environment_setup, ci_release_deployment, observability, security_data, process_collaboration, locality_contracts) each with a one-paragraph description; src/rubric/repo-scope.yaml with the 44 repo-scope criteria; src/rubric/app-scope.yaml with the 46 app-scope criteria. Every entry carries the exact id, scope, level, skippable flag, discoveryVia, and investigation area from the §5 tables; the 9 gate criteria (single_command_setup, type_check, unit_tests_runnable, deps_pinned, vcs_cli_tools, structured_logging, log_scrubbing, codeowners, import_cycle_detection) carry gate: true; all weights default 1. This is a clean-room de-branded authoring — copy NOTHING verbatim from notes/PRIVATE-rubric; SPEC §5 is the only source. DELIVERABLE TESTS: a test asserts counts (9 categories, 44+46=90 criteria, 70 deterministic / 20 agent, 9 gates, one per category) and that every agent criterion names one of the 4 areas. ACCEPTANCE: the step-5 loader accepts all three files with zero invariant violations. REFERENCES: SPEC §5 (the full catalog), §3.1-3.3.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:31:46.773Z","labels":["rubric"],"plan_id":"pl-974b","blocks":["trellis-0b7c","trellis-5963","trellis-4ba0","trellis-7cb8"],"closedAt":"2026-06-06T19:31:46.773Z"}
{"id":"trellis-0b7c","title":"CLI skeleton (thin commander entrypoints over the domain core, SPEC §13.1) + first end-to-end command: trellis rubric [--validate]","status":"closed","type":"feature","priority":1,"plan_step_index":6,"description":"Build the CLI skeleton under the api>cli>sdk discipline and ship the first end-to-end command. DELIVERABLES: src/cli/main.ts is a thin commander program (name trellis, version from src/index.ts VERSION) that registers subcommand modules; src/cli/ entrypoints parse args, call core functions, shape output — zero business logic (SPEC §13.1). Global conventions established: --json / --md output flags, human terminal output by default, pino logger initialization, consistent error rendering + exit codes scaffold. First real command: `trellis rubric` prints the loaded rubric summary (per-category criterion counts, levels, gates) + RUBRIC_VERSION; `trellis rubric --validate` runs the loader invariants and exits non-zero with precise errors on violation. DELIVERABLE TESTS: CLI smoke test via Bun.spawn asserting exit codes and JSON shape for rubric/--validate on the real rubric data and on a broken fixture. ACCEPTANCE: `trellis rubric --validate` is green against the step-6 catalog (SPEC §14 milestone 1 gate); bin works via `bun run src/cli/main.ts rubric`. REFERENCES: SPEC §12, §13.1, §14.1.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:37:43.989Z","labels":["rubric","cli"],"plan_id":"pl-974b","blocks":["trellis-59ea","trellis-7cb8"],"closedAt":"2026-06-06T19:37:43.989Z"}
{"id":"trellis-92f6","title":"Scoring engine: per-criterion score, pass rate, coverage clamp, naKind handling, 20-pt bands, repo/app aggregation — pure functions unit-tested against §3.4 worked examples","status":"closed","type":"feature","priority":1,"plan_step_index":7,"description":"Implement the scoring engine as pure, surface-agnostic core functions. DELIVERABLES: src/scoring/ exports functions implementing SPEC §3.4 exactly: perCriterionScore = numerator/denominator with null (N/A) excluded; passRate = mean over counted criteria; coverage = counted / (counted + no-detector + skipped); band() mapping 20-pt bands (L1 0-20 … L5 80-100); level = min(passRateLevel, coverageLevel) — the clamp is monotonic, only ever lowers. naKind discipline: not-applicable excluded from the coverage base, no-detector counted against coverage. Repo/app aggregation: app-scope criteria roll N per-app DetectorResults into numerator=passing-apps / denominator=N; repo-scope denominator always 1. gate/weight fields are read into types but NOT consulted by the v0 scorer (reserved per SPEC §3.3). DELIVERABLE TESTS: worked examples covering every band boundary, clamp no-op at full coverage, clamp lowering on thin coverage, both naKind paths, multi-app aggregation, and the §6.2 scorecard-entry shape (numerator/denominator/rationale ≤500 chars/naKind). ACCEPTANCE: bun test green; functions are pure (no I/O). REFERENCES: SPEC §3.2-3.4, §6.2.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:45:06.084Z","labels":["scoring"],"plan_id":"pl-974b","blocks":["trellis-59ea","trellis-7cb8"],"closedAt":"2026-06-06T19:45:06.084Z"}
{"id":"trellis-5963","title":"Detector infrastructure: DetectionContext + DetectorResult contract (SPEC §8.1), criterion→detector registry, not-applicable vs no-detector discipline","status":"closed","type":"feature","priority":1,"plan_step_index":8,"description":"Build the deterministic detector infrastructure. DELIVERABLES: src/detectors/ exports the SPEC §8.1 contract verbatim: DetectionContext { repoPath, app: { path, languages }, run(argv, opts), readFile(rel), glob(pattern) } and DetectorResult { numerator: number|null, denominator: 1, naKind?, rationale ≤500 chars }; type Detector = (ctx) => Promise<DetectorResult>. src/detectors/registry.ts binds criterion id → detector with per-language adapter resolution (a criterion may bind common, per-language, or be unbound); an unbound deterministic criterion resolves to no-detector with a clear rationale — never a crash, never a false pass. The run() helper executes subprocesses sandboxed to the target repo cwd with timeouts and captures exit/stdout/stderr; nothing in any detector may write to the target repo (read-only mandate). Discipline encoded as helpers: notApplicable(reason) for honestly-absent surfaces vs noDetector(reason) for should-have-measured-but-couldn't (tool missing, ambiguous evidence). DELIVERABLE TESTS: registry resolution precedence, unbound-criterion fallback, run() timeout/error mapping, both helper paths. ACCEPTANCE: bun test green; registry covers all 70 deterministic criterion ids (bound or explicit no-detector stub). REFERENCES: SPEC §8.1, §3.2.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:51:11.454Z","labels":["detectors"],"plan_id":"pl-974b","blocks":["trellis-a3b2","trellis-d600","trellis-299e","trellis-3d67","trellis-89b4","trellis-7f70","trellis-7cb8"],"closedAt":"2026-06-06T19:51:11.454Z"}
{"id":"trellis-a3b2","title":"App discovery (SPEC §8.2): find independently-deployable directories as apps; 0 found → repo root is 1 app; app map recorded in the report","status":"closed","type":"feature","priority":1,"plan_step_index":9,"description":"Implement app discovery. DELIVERABLES: src/discovery/ finds independently-deployable directories as apps per SPEC §8.2: package manifests (package.json with bin/main, pyproject.toml, Package.swift), build files, service dirs; nested manifests under node_modules/vendor/build outputs excluded. If 0 apps found, the repo root is exactly 1 app. Each discovered app carries { path, languages[] } with language detection (ts/js via tsconfig/package.json, swift via Package.swift, python via pyproject/setup.cfg), honoring the optional `languages` hint from targets.yaml when present. The app map feeds the report's §6.3 `apps` object ({ path: { description } }). DELIVERABLE TESTS: fixture trees for single-app root, multi-app monorepo (warren-like src/ui + server), hint override, and exclusion rules. ACCEPTANCE: bun test green; discovery is pure given a filesystem (deterministic ordering — sort by path). REFERENCES: SPEC §8.2, §6.3, §6.5.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T19:56:08.300Z","labels":["detectors"],"plan_id":"pl-974b","blocks":["trellis-59ea","trellis-7cb8"],"closedAt":"2026-06-06T19:56:08.300Z"}
{"id":"trellis-d600","title":"Common language-agnostic detectors: gitignore, env template, CI/workflow presence, CODEOWNERS, branch protection via VCS CLI, dependabot, issue/PR templates, release automation, and the rest of detectors/common/","status":"closed","type":"feature","priority":1,"plan_step_index":10,"description":"Implement the common (language-agnostic) deterministic detectors in src/detectors/common/. DELIVERABLES: detectors bound in the registry for at least: env_template (.env.example-style file committed), gitignore_comprehensive, deps_pinned (lockfile committed + pinned manifest), devcontainer (presence), vcs_cli_tools (gh/glab on PATH + authenticated probe), monorepo_tooling (S), dependency_update_automation (dependabot/renovate config), release_notes_automation, release_automation (release/publish workflow), version_drift_detection (S), codeowners, issue_templates, issue_labeling_system, pr_templates, automated_pr_review (S), backlog_health (S), branch_protection (S, via VCS CLI), automated_security_review (S), secret_scanning (S), min_release_age (dependabot cooldown), privacy_compliance (S), pre_commit_hooks, fast_ci_feedback (S), build_performance_tracking (S), deployment_frequency (S), progressive_rollout (S), rollback_automation (S), feature_flag_infrastructure, dead_feature_flag_detection (S), large_file_detection, tech_debt_tracking. Every detector returns honest naKind per the §3.2 split and a concrete rationale naming the evidence path/command. DELIVERABLE TESTS: fixture-driven per detector (pass, fail, and N/A fixtures where skippable). ACCEPTANCE: bun test green; each listed criterion id resolves through the registry to this module for any language. REFERENCES: SPEC §5.4, §5.5, §5.7, §5.8, §8.1.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T20:09:50.303Z","labels":["detectors"],"plan_id":"pl-974b","blocks":["trellis-59ea","trellis-7cb8"],"closedAt":"2026-06-06T20:09:50.303Z"}
{"id":"trellis-299e","title":"TypeScript adapter detectors: Biome, tsc strict flags, knip, jscpd, bun test, coverage ratchets, import cycles, and the TS-specific Locality & Contracts checks","status":"closed","type":"feature","priority":1,"plan_step_index":11,"description":"Implement the TypeScript language adapter in src/detectors/lang/typescript/. DELIVERABLES: detectors for lint_config (Biome/eslint with real rules), type_check (tsc configured + clean), formatter, strict_typing (strict + noImplicitAny), naming_consistency (Biome filenaming), dead_code_detection (knip), duplicate_code_detection (jscpd under threshold), unused_dependencies_detection (knip), code_modularization (S, import-direction), cyclomatic_complexity (Biome complexity caps), unit_tests_runnable (bun test/vitest/jest runs real tests — not a no-op: assert ≥1 test executed), test_coverage_thresholds (coverage configured AND enforced — ratchet script or config threshold), structured_logging (pino/winston module), error_tracking_contextualized, plus the Locality & Contracts TS bindings: import_cycle_detection (madge/knip acyclic), orphan_module_detection, explicit_any_detection (lint rule as error), strictest_type_checking (noUncheckedIndexedAccess etc. beyond baseline), greppable_exports (no default exports), barrel_file_reexport_detection (no export *), mutation_testing (S, StrykerJS), machine_checked_architecture (S). Tools invoked via the target repo's own config; configured-but-unavailable → no-detector; concept-absent → not-applicable. DELIVERABLE TESTS: fixture mini-repos per detector exercising pass/fail/N-A. ACCEPTANCE: bun test green; every TS-applicable deterministic criterion id binds. REFERENCES: SPEC §5.2, §5.3, §5.6, §5.9, §8.3 table.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T20:26:53.632Z","labels":["detectors","typescript"],"plan_id":"pl-974b","blocks":["trellis-59ea","trellis-7cb8"],"closedAt":"2026-06-06T20:26:53.632Z"}
{"id":"trellis-59ea","title":"Report renderers (terminal/JSON/MD, §6.3 shape) + trellis audit <repo-path> det-only end-to-end against warren — agent criteria resolve no-detector for now","status":"closed","type":"feature","priority":1,"plan_step_index":12,"description":"Ship report renderers and the first end-to-end audit. DELIVERABLES: src/report/ renders the SPEC §6.3 report shape three ways — terminal (per-category table, level banner, coverage, N/A breakdown), --json (exact §6.3 document), --md (scorecard suitable for pasting into a PR/issue). `trellis audit <repo-path>` wires the full det-only pipeline in core: discovery → registry → detectors (per app + repo) → scoring → report; agent-discovery criteria resolve to no-detector('investigation layer not yet wired') so coverage honestly reflects the gap. Flags: --json/--md, --rubric-version (informational), exit code 0 for now (the --fail-on contract lands in the SDK/exit-codes step). DELIVERABLE TESTS: golden-snapshot the JSON and MD renderers against a synthetic scorecard; an integration test audits a small fixture repo end-to-end. ACCEPTANCE: `trellis audit ../warren` (or any warren-stack checkout) completes, prints a coherent scorecard, and the same invocation twice produces byte-identical JSON output (determinism). This closes SPEC §14 milestone 3. REFERENCES: SPEC §6.2, §6.3, §12, §14.3.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T20:40:34.238Z","labels":["cli","report"],"plan_id":"pl-974b","blocks":["trellis-3d67","trellis-89b4","trellis-5447","trellis-ffdd","trellis-7cb8"],"closedAt":"2026-06-06T20:40:34.238Z"}
{"id":"trellis-3d67","title":"Swift adapter detectors: SwiftLint, swift build/test, swift-format, periphery, coverage — with honest not-applicable mapping for concepts Swift lacks","status":"closed","type":"feature","priority":2,"plan_step_index":13,"description":"Implement the Swift language adapter in src/detectors/lang/swift/. DELIVERABLES: detectors binding the §8.3 table column — lint_config (SwiftLint config with real rules), type_check (swift build, warnings-as-errors for strict_typing), formatter (swift-format/swiftformat), unit_tests_runnable (swift test executes ≥1 real test), test_coverage_thresholds (swift test --enable-code-coverage + enforced threshold), dead_code_detection (periphery), duplicate_code_detection (jscpd), cyclomatic_complexity (SwiftLint complexity rules), naming_consistency (SwiftLint naming rules). Criteria with no Swift analogue (unused_dependencies_detection, greppable_exports, barrel_file_reexport_detection, explicit_any_detection, import_cycle_detection if no tool is bound, mutation_testing via muter when configured) resolve to not-applicable with a rationale naming the language gap — never silently skipped. Tool-configured-but-missing → no-detector. DELIVERABLE TESTS: fixture Swift package trees per detector (pass/fail/N-A); tests must not require a Swift toolchain on CI — probe and skip-as-fixture where needed. ACCEPTANCE: bun test green; auditing a Swift fixture yields honest N/A accounting per §3.2. REFERENCES: SPEC §8.3, §3.2, §14.4.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T20:50:56.441Z","labels":["adapters","swift"],"plan_id":"pl-974b","blocks":["trellis-a534","trellis-7cb8"],"closedAt":"2026-06-06T20:50:56.441Z"}
{"id":"trellis-89b4","title":"Python adapter detectors: ruff, mypy --strict, pytest, coverage.py, vulture, deptry, grimp/pydeps — with honest not-applicable mapping","status":"closed","type":"feature","priority":2,"plan_step_index":14,"description":"Implement the Python language adapter in src/detectors/lang/python/. DELIVERABLES: detectors binding the §8.3 table column — lint_config (ruff with real rules), type_check (mypy configured + clean), strict_typing (mypy --strict), formatter (ruff format/black), unit_tests_runnable (pytest executes ≥1 real test), test_coverage_thresholds (coverage.py + enforced threshold e.g. fail_under), dead_code_detection (vulture), duplicate_code_detection (jscpd/pylint dup), unused_dependencies_detection (deptry), cyclomatic_complexity (radon/ruff C901), import_cycle_detection (grimp/pydeps acyclic), orphan_module_detection. TS-specific Locality criteria (greppable_exports, barrel_file_reexport_detection, explicit_any_detection, strictest_type_checking beyond mypy --strict) resolve to not-applicable; mutation_testing binds mutmut when configured. Tool-configured-but-missing → no-detector. DELIVERABLE TESTS: fixture pyproject trees per detector (pass/fail/N-A), no Python toolchain required on CI (probe + fixture discipline as in the Swift adapter). ACCEPTANCE: bun test green; auditing a Python fixture yields honest N/A accounting. REFERENCES: SPEC §8.3, §3.2, §14.4.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T21:04:09.686Z","labels":["adapters","python"],"plan_id":"pl-974b","blocks":["trellis-a534","trellis-7cb8"],"closedAt":"2026-06-06T21:04:09.686Z"}
{"id":"trellis-5447","title":"SQLite store (bun:sqlite): schema.sql + migrations for runs / criterion_results / investigation_cache (SPEC §6.4), audit persistence wired in","status":"closed","type":"feature","priority":1,"plan_step_index":15,"description":"Implement the central SQLite store. DELIVERABLES: src/store/ uses bun:sqlite with schema.sql + sequential migrations under src/store/migrations/ creating exactly the SPEC §6.4 tables: runs (id, repo, commit_sha, rubric_version, level, pass_rate, coverage, report_json, scored_at; index runs_repo_time), criterion_results (run_id FK, criterion, numerator nullable, denominator, na_kind, rationale; index cr_run), investigation_cache (PK repo+commit_sha+area, findings_json, created_at). A store module exposes typed functions: openStore(dbPath) with migration-on-open, insertRun(report) writing the runs row + exploded criterion_results in one transaction, latestRun(repo), runsSince(repo, date), cache get/put for investigations. DB location: a trellis-owned path (e.g. ~/.trellis/trellis.db) overridable via env/flag — never inside an audited repo (central-state non-goal, SPEC §2). `trellis audit` now persists each run. DELIVERABLE TESTS: migration idempotency, transactional insert, query round-trips against a temp DB. ACCEPTANCE: bun test green; two audits of the same fixture produce two runs rows with identical report_json. REFERENCES: SPEC §6.4, §2 (central state), §11.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T21:11:01.125Z","labels":["store"],"plan_id":"pl-974b","blocks":["trellis-4222","trellis-6eb1","trellis-dde8","trellis-7cb8"],"closedAt":"2026-06-06T21:11:01.125Z"}
{"id":"trellis-4ba0","title":"Investigation layer core: the 4 fixed areas (areas.ts), zod findings schemas per area (findings.ts), and the pure deterministic grader (facts → pass/fail/N-A, SPEC §7)","status":"closed","type":"feature","priority":1,"plan_step_index":16,"description":"Build the investigation core: areas, findings contracts, and the deterministic grader. DELIVERABLES: src/investigation/areas.ts defines the 4 fixed areas (documentation, agent-config, setup-runnability, test-layout) each with id, description, and the per-area investigation prompt scaffold (what facts to gather, read-only, call submit_findings exactly once, facts never verdicts) per SPEC §7.1/§9.2. src/investigation/findings.ts defines one zod schema per area capturing objective facts only (the AgentConfigFindings example in SPEC §7.2 is the shape model — finalize all four: documentation files/freshness/build-cmd/runbooks/arch-docs; agent instruction files + skills + validation automation + co-authorship count; secrets mechanism + scripted services + devcontainer buildability evidence; test dirs/naming/integration boundaries/isolation/timing surfaces). src/investigation/grader.ts is pure and deterministic: for each of the 20 agent criteria, a function from the bound area's Findings (+ skippable rules) to { numerator, denominator, naKind?, rationale } — e.g. agents_md passes iff some instruction file has all four booleans; skills passes iff ≥1 skill has name+description+non-empty prompt. Same facts → same grade, byte-identical rationale. DELIVERABLE TESTS: per-criterion grader tables (facts → expected grade) covering pass/fail/N-A for all 20 agent criteria; schema rejection of verdict-shaped extras. ACCEPTANCE: bun test green; all 20 agent criterion ids map to a grader function. REFERENCES: SPEC §7.1-7.2, §5 (agent-tagged rows).","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T21:20:56.670Z","labels":["investigation"],"plan_id":"pl-974b","blocks":["trellis-98b5","trellis-7cb8"],"closedAt":"2026-06-06T21:20:56.670Z"}
{"id":"trellis-98b5","title":"Pi RPC provider (SPEC §9): argv builder, findings-extension.ts (submit_findings tool), JSONL event loop, env passthrough, corrective-prompt retries, heartbeat watchdog, version probe + graceful no-detector degradation","status":"closed","type":"feature","priority":1,"plan_step_index":17,"description":"Implement the Pi RPC provider — the single transport through which facts are produced. DELIVERABLES: src/investigation/provider/ exposes investigate(repoPath, area, opts): Promise<Findings> (SPEC §9.0 core entrypoint). buildPiArgv mirrors burrow (../burrow/src/runtime/pi.ts reference): pi --mode rpc --no-session --no-extensions -e <findings-extension> --offline --no-context-files --provider <p> --model <m> --tools <read-only builtins>,submit_findings --system-prompt <per-area prompt>, cwd = target repo. src/investigation/provider/pi/findings-extension.ts registers the submit_findings tool whose parameter JSON-schema derives from the area's zod schema via zod-to-json-schema. Protocol per SPEC §9.3: write one prompt line, HOLD STDIN OPEN, watch stdout JSONL for the submit_findings toolCall, zod-validate arguments, close stdin on capture; on missing/invalid call send one corrective prompt echoing zod errors, bounded to N retries (default 2); exhaustion/timeout/stopReason:error → resolve no-detector with the failure rationale, never a fabricated pass. Heartbeat watchdog force-terminates stalls. Provider/model selection precedence: CLI flags > targets.yaml defaults.investigation > one documented constant; provider names lowercased. Env passthrough only (never argv): ANTHROPIC_API_KEY/AUTH_TOKEN/BASE_URL always + the PI_PROVIDER_ENV_KEYS map (openai/google/groq/mistral/deepseek) per §9.4. Startup probe: pi --version against the documented supported version; missing/incompatible Pi degrades every agent criterion to no-detector with a hint — never crashes the audit. DELIVERABLE TESTS: argv construction, env filtering, retry/corrective flow, watchdog, and degradation — all against a scripted fake pi process (no live calls). ACCEPTANCE: bun test green with zero network/model access. REFERENCES: SPEC §9.0-9.6.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T21:40:21.885Z","labels":["investigation","pi"],"plan_id":"pl-974b","blocks":["trellis-f478","trellis-7cb8"],"closedAt":"2026-06-06T21:40:21.885Z"}
{"id":"trellis-f478","title":"Golden fixtures + offline test harness (SPEC §9.7): captured + canonicalized Pi RPC sessions per area under __golden__/, parser → zod → grader exercised fully offline, TRELLIS_UPDATE_PI_GOLDEN regeneration gate","status":"closed","type":"feature","priority":1,"plan_step_index":18,"description":"Create the golden-fixture suite for the investigation layer. DELIVERABLES: src/investigation/__golden__/ holds one captured real `pi --mode rpc` session per area (full JSONL event stream including the submit_findings toolCall), canonicalized — timestamps, responseId, session/tool-call ids, usage normalized to fixed placeholders — and frozen. A capture/regeneration script (scripts/ or test helper) re-records goldens ONLY when TRELLIS_UPDATE_PI_GOLDEN=1 AND an explicit live-call flag are both set; otherwise it refuses. Unit tests run the full chain offline: RPC parser → toolCall capture → zod validation → deterministic grader, asserting the exact grades each golden produces; a corrupted-golden fixture exercises the corrective/no-detector path. CI configuration makes no model calls anywhere (assert via test that no network env is required). ACCEPTANCE: bun test green offline with PI/API env vars unset; regeneration script documented in AGENTS.md/RUNBOOK; goldens stable across two consecutive test runs. NOTE: the initial capture requires one live Pi run per area by the operator — coordinate with the user if no capture exists yet; until then hand-authored fixtures matching the real wire shape (validated against burrow's documented v0.74.0 envelope) are acceptable and marked as such. REFERENCES: SPEC §9.7, burrow golden methodology.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T21:51:43.723Z","labels":["investigation","testing"],"plan_id":"pl-974b","blocks":["trellis-4222","trellis-7cb8"],"closedAt":"2026-06-06T21:51:43.723Z"}
{"id":"trellis-4222","title":"Wire investigation into audit: investigate() core entrypoint, investigation_cache keyed (repo, commit_sha, area), --no-cache flag, agent criteria flow into the scorecard","status":"closed","type":"feature","priority":1,"plan_step_index":19,"description":"Wire the investigation layer into the audit pipeline with caching. DELIVERABLES: the audit core, for each of the 4 areas referenced by any to-be-scored agent criterion, resolves findings via: investigation_cache lookup keyed (repo, commit_sha, area) → on miss, investigate() (Pi provider) → zod-validate → cache write; --no-cache forces re-investigation (SPEC §7.3/§9.5). Grader output for all 20 agent criteria flows into the scorecard alongside deterministic results; rationale strings name the area + key facts. Areas run once per repo per commit regardless of how many criteria consume them (bounded LLM cost). Pi-unavailable or area-failure paths surface as no-detector entries counted against coverage. commit_sha resolution: git rev-parse HEAD of the target; dirty worktrees recorded with a -dirty suffix so cache keys never lie. DELIVERABLE TESTS: cache hit/miss/no-cache flows against a temp DB with a stubbed provider; an end-to-end audit of a fixture repo where agent criteria score from cached findings, asserting byte-identical scorecards across two runs at the same commit. ACCEPTANCE: bun test green; `trellis audit` on a fixture exercises det + agent criteria together; re-run at same commit is byte-identical. REFERENCES: SPEC §7.3, §9.5, §3.2.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T22:06:17.723Z","labels":["investigation"],"plan_id":"pl-974b","blocks":["trellis-a534","trellis-7cb8"],"closedAt":"2026-06-06T22:06:17.723Z"}
{"id":"trellis-dd9c","title":"Canonical standards set: src/standards/canonical/ files extracted from the l5-toolkit/warren conventions + manifest.yaml with per-file semver, hashes, and matcher kinds (SPEC §10)","status":"closed","type":"feature","priority":1,"plan_step_index":20,"description":"Author the canonical standards set and manifest. DELIVERABLES: src/standards/canonical/ contains the bundled canonical files per SPEC §10, extracted from the current os-eco conventions (templates/l5-toolkit + warren as reference): biome.json, tsconfig.base.json, .github/workflows/ci.yml, AGENTS.md (template/section-aware), scripts/hooks/pre-commit, .seeds/config.yaml — plus any additional files the l5-toolkit treats as canonical (knip.json, .jscpd.json, bunfig.toml, dependabot.yml) where stable. src/standards/manifest.yaml describes the set: top-level version (start \"1.0.0\"), and per file { path, version, hash: sha256, matcher: exact|json-subset|yaml-subset|text|template }. A small script or test recomputes hashes so manifest drift inside trellis itself fails CI. DELIVERABLE TESTS: manifest schema validation (zod), hash agreement between manifest.yaml and the canonical files on disk, matcher kind coverage. ACCEPTANCE: bun test green; `trellis standards` (landing next step) will read this manifest; every canonical file is byte-tracked. REFERENCES: SPEC §10, templates/l5-toolkit/configs+github, warren root configs.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T22:16:47.241Z","labels":["drift"],"plan_id":"pl-974b","blocks":["trellis-ffdd","trellis-7cb8"],"closedAt":"2026-06-06T22:16:47.241Z"}
{"id":"trellis-ffdd","title":"Drift engine: drift.ts matchers (exact/json-subset/yaml-subset/text/template), allowed-deltas, per-file states (match/allowed-delta/drift/missing/extra), trellis drift + trellis standards commands, report.drift","status":"closed","type":"feature","priority":1,"plan_step_index":21,"description":"Implement the drift engine and its CLI commands. DELIVERABLES: src/standards/drift.ts compares a target repo against the canonical set: per-file matchers exact (byte), json-subset (canonical keys must be present/equal, target may extend), yaml-subset, text (normalized whitespace), template (section-aware for AGENTS.md — required sections present, boilerplate matched); per-file output state match | allowed-delta | drift | missing | extra. Allowed deltas honor the targets.yaml shape (file-level and structural paths within a file, each with a reason) — whitelisted divergences report allowed-delta, not drift. The target's tracked canonical version resolves per-repo override > defaults.canonicalVersion. `trellis drift <repo-path>` runs drift standalone (accepts --canonical <v>; without fleet context allowedDeltas default empty); `trellis standards` prints the canonical manifest + versions. Drift results fold into the audit report under report.drift (SPEC §6.3). DELIVERABLE TESTS: fixture repos per matcher kind and per state (match/allowed/drift/missing/extra), structural-path allow-listing, version override resolution. ACCEPTANCE: bun test green; `trellis drift` on a warren-stack checkout produces a coherent per-file table in terminal/JSON/MD. REFERENCES: SPEC §10, §6.5, §12.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T22:32:43.554Z","labels":["drift","cli"],"plan_id":"pl-974b","blocks":["trellis-6eb1","trellis-28a5","trellis-7cb8"],"closedAt":"2026-06-06T22:32:43.554Z"}
{"id":"trellis-6eb1","title":"Fleet: targets.yaml loader (defaults, per-target canonical overrides + allowedDeltas, skip, osecoDetectors, languages) + trellis fleet multi-repo orchestration","status":"closed","type":"feature","priority":1,"plan_step_index":22,"description":"Implement fleet orchestration. DELIVERABLES: src/fleet/ loads and zod-validates targets.yaml per SPEC §6.5: defaults { canonicalVersion, investigation.{provider,model} } and targets[] { id, path, languages?, canonical?: { version?, allowedDeltas? }, skip?: criterion ids forced to not-applicable, osecoDetectors?: boolean }. Path resolution relative to the targets.yaml location; missing/unreadable target paths fail that target with a clear per-target error without aborting the fleet. `trellis fleet [--targets targets.yaml] [--json|--md]` audits every target sequentially (audit + drift with the target's allowedDeltas and skip list applied), persists each run to the central store, and renders an aggregate dashboard: per-repo level, pass rate, coverage, drift counts, and level deltas vs each repo's previous run. The skip list maps to not-applicable with rationale 'skipped via targets.yaml'. DELIVERABLE TESTS: targets schema validation fixtures, skip/osecoDetectors/allowedDeltas plumbing into the audit context, partial-failure isolation, aggregate rendering snapshot. ACCEPTANCE: bun test green; a two-fixture-target fleet run writes two runs rows and prints the aggregate table. REFERENCES: SPEC §6.5, §12, §14.7.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T22:48:43.847Z","labels":["fleet","cli"],"plan_id":"pl-974b","blocks":["trellis-dde8","trellis-28a5","trellis-a534","trellis-7cb8"],"closedAt":"2026-06-06T22:48:43.847Z"}
{"id":"trellis-dde8","title":"History reporting: trellis report dashboard from SQLite (--repo/--since/--json/--md) + changes-since-last-run deltas with rubric-version attribution (SPEC §11)","status":"closed","type":"feature","priority":1,"plan_step_index":23,"description":"Implement history reporting and change attribution. DELIVERABLES: `trellis report [--repo <id>] [--since <date>] [--json|--md]` renders from the central SQLite store: per-repo run history (level, pass rate, coverage over time), fleet snapshot of latest runs, and per-criterion trend queries powered by criterion_results. changesSinceLastRun (SPEC §11): comparing a run against the most recent prior run for the same repo, list per-criterion transitions (pass→fail, fail→pass, naKind changes, app-count/denominator changes) and the net level move; embed in the §6.3 report at audit time and render in `trellis report`. Rubric attribution: when the two runs' rubric_version differ, the delta is flagged explicitly as possibly-rubric-driven; identical versions mark it a real code regression/improvement. DELIVERABLE TESTS: seeded multi-run temp DBs asserting delta computation across all transition kinds, the rubric-version flag both ways, --since filtering, and renderer snapshots. ACCEPTANCE: bun test green; after two fleet runs of a changed fixture, `trellis report` shows the criterion-level diff with correct attribution. REFERENCES: SPEC §11, §6.3, §6.4, §12.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T23:11:36.765Z","labels":["fleet","report","cli"],"plan_id":"pl-974b","blocks":["trellis-28a5","trellis-7cb8"],"closedAt":"2026-06-06T23:11:36.765Z"}
{"id":"trellis-7f70","title":"os-eco-native detectors (SPEC §8.4): seeds/mulch/canopy/plot/skills/check:all/ratchet evidence mapped onto existing criteria, osecoDetectors per-target toggle","status":"closed","type":"feature","priority":2,"plan_step_index":24,"description":"Implement the os-eco-native detector pack in src/detectors/oseco/. DELIVERABLES: evidence detectors that strengthen EXISTING criteria (no new category, SPEC §8.4), active by default and disabled when the target sets osecoDetectors: false: AGENTS.md+CLAUDE.md richness → agents_md; */SKILL.md → skills; .seeds/ presence + labeling config (+ sd data when available) → issue_templates / issue_labeling_system / backlog_health; check:all script + CI invoking it verbatim → unit_tests_runnable / pre_commit_hooks / fast_ci_feedback evidence; ratchet scripts (file-size/debt/coverage/complexity/bundle/jscpd/knip) → large_file_detection / tech_debt_tracking / code_quality_metrics / dead_code_detection / duplicate_code_detection / unused_dependencies_detection / heavy_dependency_detection; gen:openapi + docs/openapi.yaml → api_schema_docs; gen:docs → automated_doc_generation; .canopy/ → automated_doc_generation evidence; .mulch/ → documentation_freshness / agentic_development; .plot/ → agentic_development; agent co-author trailers in git log → agentic_development. Composition rule: oseco evidence merges with (never replaces) the common/language detector verdicts — pass if either passes; rationale names the oseco evidence path. DELIVERABLE TESTS: fixture repos with/without each surface, the toggle, and merge semantics. ACCEPTANCE: bun test green; auditing a warren-stack fixture with the pack on measurably raises honest pass rates vs off. REFERENCES: SPEC §8.4, §6.5.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-06T23:31:37.968Z","labels":["oseco","detectors"],"plan_id":"pl-974b","blocks":["trellis-a534","trellis-7cb8"],"closedAt":"2026-06-06T23:31:37.968Z"}
{"id":"trellis-28a5","title":"Typed SDK (src/client/) mirroring core types + CLI exit-code contract (--fail-on level|gate|drift|none) so trellis is CI-usable per repo (SPEC §12, §13.1)","status":"closed","type":"feature","priority":1,"plan_step_index":25,"description":"Ship the typed SDK and the CI-usable exit-code contract. DELIVERABLES: src/client/ exports a typed programmatic client (SPEC §13.1): audit(repoPath, opts), drift(repoPath, opts), fleet(targetsPath, opts), report(query), rubric() — each calling the SAME core functions the CLI does, in-process, with request/response types that mirror the core's exported types (annotated // Mirrors src/<x>). No logic in the SDK beyond type shaping. Exit-code contract across all CLI commands (SPEC §12): 0 clean; non-zero when a gate criterion fails or drift is detected, tunable via --fail-on level|gate|drift|none (document the default; level compares against a --min-level threshold). Sync enforcement: golden snapshots of the stable output shapes (report JSON, drift JSON) live in tests so surface drift turns CI red; strict tsc over the mirrored types. DELIVERABLE TESTS: SDK and CLI invocations of the same audit produce deep-equal reports (one code path proof); each --fail-on mode's exit code asserted via spawned CLI on fixtures. ACCEPTANCE: bun test + check:all green; README/AGENTS.md document the SDK entrypoints and exit codes. REFERENCES: SPEC §12, §13.1.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-07T00:00:56.116Z","labels":["cli","sdk"],"plan_id":"pl-974b","blocks":["trellis-a534","trellis-7cb8"],"closedAt":"2026-06-07T00:00:56.116Z"}
{"id":"trellis-a534","title":"Dogfood gate: trellis audits itself (must band L4+) and the warren-stack fleet end-to-end; iterate detector bindings on what actually mattered","status":"closed","type":"task","priority":1,"plan_step_index":26,"description":"Run the dogfood gate. DELIVERABLES: a real targets.yaml at the operator's machine paths covering trellis itself + the warren-stack repos (warren, burrow, plot, mulch, seeds, canopy, sapling); `trellis fleet` completes end-to-end (deterministic + cached investigation criteria + drift + oseco pack); `trellis audit .` on trellis itself bands L4 or higher — if it doesn't, file and fix the honest gaps in trellis's own repo standards (this step includes that remediation) or fix dishonest detectors. Iterate detector bindings on what the fleet run teaches: every no-detector result is triaged into (a) detector bug → fix, (b) genuinely missing adapter → leave counted, or (c) honestly absent surface → reclassify not-applicable; record decisions via ml record. Two consecutive fleet runs at unchanged commits must be byte-identical per repo (determinism check at fleet scale). DELIVERABLES (artifacts): the triage notes land as seeds issues or mulch records; detector fixes land as commits with tests. ACCEPTANCE: trellis self-audit ≥ L4; fleet dashboard renders all targets; check:all green; the SPEC §14 dogfood gate is closed. REFERENCES: SPEC §13 (dogfood mandate), §14 (gate).","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-07T01:11:36.361Z","labels":["dogfood"],"plan_id":"pl-974b","blocks":["trellis-3447","trellis-7cb8"],"closedAt":"2026-06-07T01:11:36.361Z"}
{"id":"trellis-3447","title":"Release v0.1.0: CHANGELOG roll, version sync (package.json + src VERSION), publish @os-eco/trellis-cli via the version-gated workflow","status":"closed","type":"task","priority":1,"plan_step_index":27,"description":"Cut the v0.1.0 release. DELIVERABLES: execute .claude/commands/release.md — review git log since repo birth, bump package.json version AND src/index.ts VERSION to 0.1.0 (the publish workflow fails on mismatch), roll CHANGELOG.md [Unreleased] into a dated 0.1.0 section summarizing the MVP surface (audit/drift/fleet/report/rubric/standards commands, 3 language adapters, Pi investigation layer, canonical drift, SQLite history), refresh README/CLAUDE.md stats if command counts changed, commit. Verify the publish path: workflows/publish.yml is version-gated against npm view @os-eco/trellis-cli (package not yet on npm → publishes on first main push once NPM_TOKEN is configured; coordinate with the operator for the token — see ../scripts/set-npm-token.sh at the os-eco root). Final gate: bun test, bun run lint, bun run typecheck, bun run check:all all exit 0; sd close completed plan children; ml record release decision; sd sync. ACCEPTANCE: a release commit exists on main with synced versions and rolled changelog; CI green; npm publish succeeds or is verified blocked only on the operator-held token. REFERENCES: .claude/commands/release.md (authored in step 2), seeds/.github/workflows/publish.yml pattern.","createdAt":"2026-06-06T18:41:22.565Z","updatedAt":"2026-06-11T04:15:07.223Z","labels":["release"],"plan_id":"pl-974b","blocks":["trellis-7cb8"],"closedAt":"2026-06-11T04:15:07.223Z"}
{"id":"trellis-9b72","title":"Audit run observability + report file export (--output)","status":"closed","type":"feature","priority":2,"createdAt":"2026-06-07T05:46:22.234Z","updatedAt":"2026-06-07T16:31:15.256Z","description":"`trellis audit .` is a black box while running and the default terminal output is too brief to act on. Two parts:\n\n## 1. Live progress indication\n\nThe audit runs 0.5–10min (investigation pass: up to 4 areas x ~2min Pi RPC sessions each) with zero output until the final report. Surface phase-level progress on stderr:\n\n- Phase transitions in auditRepo (src/report/build.ts:329): app discovery -> investigation -> detectors -> scoring.\n- Investigation events in runInvestigation (src/investigation/run.ts:145): per-area start, cache hit vs miss, Pi version probe, agent session start/end, retry attempts. Pi session loop (src/investigation/provider/pi/session.ts:263) already sees message_end/agent_end/heartbeat events — wire them to an optional progress callback in InvestigationDeps rather than logging directly from core.\n- Per-app detector pass progress (src/report/build.ts:368).\n- CLI: default to concise human progress lines on stderr (TTY-aware); add --quiet to suppress and --verbose for per-detector/per-message detail. Adopt the existing-but-unused pino logger (src/cli/logger.ts:16) or a thin progress-reporter interface owned by cli/ so core stays surface-agnostic (api>cli>sdk discipline — core emits events, CLI renders them).\n\n## 2. Report file export\n\nAll three renderers (terminal/JSON/markdown) exist but emit() (src/cli/output.ts:84) writes stdout only. Add:\n\n- --output <path> on audit: writes the report to a file. Format inferred from extension (.json -> renderJson, .md -> renderMarkdown), overridable by --json/--md. Terminal summary still prints to stdout so the run remains readable.\n- Keep stdout contract stable for piping (progress goes to stderr, never stdout).\n- SDK: mirror the option in client/ per the mirroring convention.\n\nOut of scope: enriching the default terminal report detail (separate concern), HTTP surface.\n\nTouch points: src/cli/audit.ts:28 (AuditCliOptions), src/cli/output.ts, src/report/build.ts, src/report/run.ts:48, src/investigation/run.ts, src/investigation/provider/pi/session.ts, src/client/.","closedAt":"2026-06-07T16:31:15.256Z"}
{"id":"trellis-89d6","title":"Make markdown/json report file render the full audit (terminal stays brief)","status":"closed","type":"feature","priority":2,"createdAt":"2026-06-07T16:46:22.855Z","updatedAt":"2026-06-07T17:17:23.614Z","description":"Refines the original enrich-report ask after dogfooding feedback. DECISION: the terminal/TUI scorecard (src/report/terminal.ts) is good as-is and should NOT get richer — leave the level banner + per-category table + N/A tally untouched, no --detail flag for terminal. The gap is the persisted report FILE: src/report/markdown.ts (37 lines) only mirrors the terminal aggregates, so the written report is useless for acting on a score (you cannot see which of the 90 criteria passed/failed/why). Make the markdown (and confirm json) FILE output render literally everything the audit looked at and graded — maximal detail, no flag, always full.\n\nMarkdown must render, grouped by category with failing criteria surfaced first:\n- every criterion: id, title, category, verdict (pass/fail/n-a vs no-detector via naKind), numerator/denominator\n- the per-criterion rationale string (the WHY, =500 chars) from ScorecardEntry\n- which gate criterion(s) failed (e.g. single_command_setup) via failingGateIds/gateFails (src/report/assess.ts, src/scoring/gate.ts) — currently only on stderr\n- report.drift (canonical-config drift) and report.changesSinceLastRun (per-criterion transitions) — present on the object, never rendered\n\nJSON: src/report/json.ts is JSON.stringify(report) so it already carries report.criteria + drift + changes; verify nothing detailed is dropped and keep it complete. Data is fully available today (report.criteria: Record<string,ScorecardEntry>, src/report/types.ts:48; entry shape src/scoring/entry.ts:27-32) — this is purely a renderer gap, no new collection needed. Touch points: src/report/markdown.ts, src/report/rollup.ts (may need a per-criterion projection helper), src/report/types.ts, golden snapshots. Out of scope: terminal changes, remediation hints.","labels":["report","cli"],"closedAt":"2026-06-07T17:17:23.614Z"}
{"id":"trellis-c2af","title":"CLI tests litter the real repo .trellis/ — default report output is cwd-relative","status":"closed","type":"bug","priority":2,"createdAt":"2026-06-07T17:09:25.143Z","updatedAt":"2026-06-07T17:25:51.514Z","description":"Symptom: after running bun test, the real repo's .trellis/ fills with timestamped audit-*.md reports (25 observed). These are NOT from a user audit run — each file's H1 names a test temp dir (trellis-cli-exit-*, trellis-cli-audit-*, trellis-sdk-*).\n\nRoot cause (two parts):\n1. src/cli/audit.ts planReportTarget (~lines 167-181, added in commit ca3e99d): when --output is omitted, the DEFAULT report target is join(process.cwd(), '.trellis') — anchored to the CURRENT WORKING DIRECTORY, not the audited repo path, and not disabled. Filenames are audit-<scoredAt-to-the-second>.md (finalizeReportPath ~185-189), so runs on different seconds accumulate instead of overwriting (N runs = N files).\n2. The CLI/SDK tests spawn 'trellis audit <tempdir>' WITHOUT setting cwd and WITHOUT --no-output, so the child inherits the real trellis repo root as cwd and writes into its .trellis/. Offenders: src/cli/exit-codes.test.ts (spawn has no cwd ~21-25; auditCli helper omits --no-output ~57-62; ~8 audit calls), src/cli/main.test.ts (~233-314), src/client/index.test.ts (~60). .gitignore:29 ignores .trellis/, so the pollution is invisible to git.\n\nNote: a single user 'trellis audit . --output .' correctly writes exactly ONE file (to cwd); it does not loop. The pile-up is purely test fallout.\n\nFix options to weigh during design: (a) make tests set cwd to a temp dir and/or pass --no-output; (b) make the default report-output anchor to the audited repo path, not process.cwd(); (c) gate default file export behind a flag / env so tests don't trigger it; (d) clean up the existing 25 stray .trellis/ files. Pick during design — at minimum tests must not write into the real repo.","labels":["cli","report","testing"],"closedAt":"2026-06-07T17:25:51.514Z"}
{"id":"trellis-8d8a","title":"Factory-ready verdict: warren dispatchability detector pack + top-line DISPATCHABLE yes/no","status":"open","type":"feature","priority":1,"createdAt":"2026-06-11T04:11:04.923Z","updatedAt":"2026-06-11T04:11:04.923Z","description":"Add detectors/oseco/warren.ts answering the binary question 'can warren dispatch against this repo today?'. Checks (all deterministic, from warren's real dispatch contract): .warren/config.yaml parses against warren's DefaultsConfigSchema (vendor or import the zod schema so trellis and warren can never disagree); qualityGate names a script that actually exists in package.json/Makefile (without it warren agents fall back to fuzzy CLAUDE.md heuristics); .seeds/ present (mandatory for plan-run dispatch); git hooks wired via prepare script core.hooksPath; default branch resolvable; runBranchPrefix/branch conventions sane. Report gains a top-line DISPATCHABLE: yes/no verdict with blocking reasons, separate from the scored rubric (an external repo must be able to hit L5 without os-eco). Lives behind the existing osecoDetectors flag."}
{"id":"trellis-788c","title":"Exec tier: trellis runs the declared quality gate and measures verify-loop wall-clock","status":"open","type":"feature","priority":1,"createdAt":"2026-06-11T04:11:28.044Z","updatedAt":"2026-06-11T04:11:28.044Z","description":"trellis currently only inspects; it should also execute. Opt-in exec tier (trellis audit --exec or a verify profile) that runs the repo's declared quality gate (.warren qualityGate, or the detected verify/check:all entrypoint), records pass/fail + wall-clock duration, and persists both in run history so trellis report shows verify-loop latency per repo over time. Makes the README credo 'any change is verifiable in under 60 seconds' an actual measured criterion instead of a slogan. Execution must run whatever gate the repo declares (never hardcode bun run check:all) to stay tool-agnostic. Support sandboxed execution via burrow when available (see runtime-smoke issue) and bare subprocess otherwise; degrade to no-detector, never false-fail, when execution is unavailable."}
{"id":"trellis-14df","title":"Runtime smoke verification: rubric criterion + execution in burrow/warren and local verify scripts","status":"open","type":"feature","priority":1,"createdAt":"2026-06-11T04:11:28.126Z","updatedAt":"2026-06-11T04:11:28.126Z","description":"The #1 fleet-wide gap found in the 14-repo scout sweep (mulch, canopy, kota-memory, kota-face, kota-voice all flagged it): gates prove code lints/typechecks/unit-tests but nothing proves the artifact actually runs — daemon boots, /health responds, CLI installs and executes, UI builds and renders. Two halves: (1) new rubric criterion (runtime_smoke_verification or similar, tool-agnostic WHAT: 'the canonical gate includes booting the built artifact and verifying liveness') graded by detectors that look for smoke steps wired into the declared verify/check:all chain; (2) an execution path — trellis (and warren runs) can execute the smoke step inside a burrow sandbox so 'does it run' is verified in isolation, both in the factory loop and locally. Pairs with the exec-tier issue; this is the criterion + sandbox story, that one is the runner + timing story."}
{"id":"trellis-9aa1","title":"trellis sync: turn audit failures and drift into seeds issues / warren plans (SPEC §15)","status":"open","type":"feature","priority":2,"createdAt":"2026-06-11T04:11:37.446Z","updatedAt":"2026-06-11T04:11:37.446Z","description":"The missing 'sync' half of 'audit & sync'. A new command (trellis sync or audit --emit-issues) that converts failed criteria and canonical-drift findings into seeds issues (or a seeds plan) in the target repo — criterion id, rationale, and remediation hint in the body — sized for autonomous work. run-plan.sh or warren plan-run dispatch then closes the loop: audit -> issues -> dispatch -> PR -> re-audit shows the level move. Drift findings are the easiest first cut (each drifted canonical file is one mechanical PR). Needs idempotency: re-running sync must not duplicate open issues (key on criterion id + rubric version)."}
{"id":"trellis-79c9","title":"Canonical standards v2: adopt kota-monorepo frozen contracts into standards/ and the rubric","status":"open","type":"feature","priority":2,"createdAt":"2026-06-11T04:11:37.507Z","updatedAt":"2026-06-11T04:11:37.507Z","description":"trellis's bundled standards/ derive from warren-era L5 toolkit; the kota fleet has evolved past it with frozen, machine-verified contracts (kota-monorepo docs/check-all-standard.md and docs/observability-standard.md). Rev canonical standards to v2: ordered GATES manifest pattern (scripts/check-all.ts with exported GATES array as single source of truth), check:ci-parity (CI may invoke nothing outside the manifest), check:boundaries (dependency-cruiser layering as hard errors), check:secrets (gitleaks), golden contract locks (check:openapi byte-diff pattern), per-incident runbooks (kota-memory ships six playbooks), and the 7-rule observability/silent-failure contract. Add matching tool-agnostic rubric criteria (ci-parity enforced, boundary enforcement, contract golden locks, runbooks per failure mode, mutation testing ratchet, test quarantine with tracker links). Dogfood consequence: trellis itself must adopt v2 to keep its L4+ self-score."}
{"id":"trellis-d02e","title":"Rubric ceiling policy: no repo scores 100% — rev the rubric when the fleet tops out","status":"open","type":"task","priority":2,"createdAt":"2026-06-11T04:11:42.217Z","updatedAt":"2026-06-11T04:11:42.217Z","description":"Policy + mechanism: a 100% score means the rubric is behind the fleet, not that the repo is done. (1) Document the policy in SPEC/README: the rubric is a ratchet on the whole fleet; when any audited repo passes every measured criterion, that is the trigger to add criteria / tighten bands in the next RUBRIC_VERSION. (2) Mechanize the signal: trellis fleet / trellis report flag any repo at 100% pass-rate (or all-gates-passing at L5) with an explicit 'rubric ceiling reached — rev the rubric' warning, and the history store should make ceiling events queryable so the dashboard surfaces them. Keeps levels meaningful as the fleet improves."}
{"id":"trellis-7bae","title":"Flaky on CI: build.oseco acceptance test times out at 5000ms on shared runners","status":"open","type":"bug","priority":2,"createdAt":"2026-06-11T04:21:42.779Z","updatedAt":"2026-06-11T04:21:42.779Z","description":"src/report/build.oseco.test.ts 'the pack on raises honest pass rates vs off (acceptance)' runs a real auditRepo twice and intermittently exceeds bun's default 5s per-test timeout on GitHub-hosted runners (failed the v0.1.0 publish run 27323397867 first attempt; passed the parallel CI run on the same commit and passes locally). Fix: raise the per-test timeout for this acceptance test (bun test supports a timeout arg per test) or slim the fixture so two full audit passes fit comfortably; also killed '1 dangling process' was logged — check the test leaves no subprocess behind. Per canonical-standards-v2 direction (trellis-79c9), a recurring flake should land in a tracked quarantine pattern, not be retried blindly."}
{"id":"trellis-589a","title":"Detectors fingerprint named tools and miss in-house implementations (false negatives on kota-memory)","status":"open","type":"bug","priority":1,"createdAt":"2026-06-11T04:54:17.350Z","updatedAt":"2026-06-11T04:54:17.350Z","description":"Fleet baseline 2026-06-11: kota-memory fails feature_flag_infrastructure with rationale 'no feature-flag system (LaunchDarkly/Unleash/OpenFeature/Flipt/...) configured' — but it has a documented, runtime-toggleable in-house flag system (KM_FEATURE_<NAME> env overrides + config, src/flags). Same pattern for structured_logging ('0/1 apps pass' — kota-memory ships an in-house zero-dep structured JSON logger per its dependency charter, no pino/winston fingerprint). The rubric WHAT is tool-agnostic but these detectors only recognize product fingerprints, so zero-dep/in-house implementations (the entire kota charter) read as absent. Fix direction: per-criterion fallback heuristics (env-var flag pattern + config docs; JSON-lines logger usage) and/or route to the investigation pass when deterministic fingerprints miss — a fact like 'repo implements X in-house' must grade pass. Also verify kota-memory's secrets_management fail ('Secrets are committed') — possibly fixture/allowlist false positive from the investigation grader."}
{"id":"trellis-98c0","title":"Investigate run-to-run score variance: kota-memory 94% vs 90% pass across back-to-back audits","status":"open","type":"task","priority":2,"createdAt":"2026-06-11T04:54:17.409Z","updatedAt":"2026-06-11T04:54:17.409Z","description":"During the 2026-06-11 fleet baseline kota-memory scored 94% pass on one run and 90% on the fleet run shortly after (same day; commit likely unchanged). Investigation results are cached keyed (repo, commit_sha, area), so two same-commit runs should be identical — either the commit differed, the cache was bypassed, or the Pi investigation pass is nondeterministic across cache misses. Reproduce: two consecutive 'trellis audit' runs of an unchanged checkout must produce identical scorecards (the determinism promise). If agent-graded criteria flip between runs, consider pinning/strengthening grader facts or surfacing a determinism warning in the report."}
{"id":"trellis-7ffe","title":"Adopt canonical check:all standard","status":"open","type":"feature","priority":2,"createdAt":"2026-06-12T04:51:00.736Z","updatedAt":"2026-06-12T05:45:19.493Z","plan_id":"pl-134e"}
{"id":"trellis-0c67","title":"Add the canonical scripts/check-all.ts quiet runner copied byte-identical from templates/l5-toolkit/scripts/check-all.ts, with trellis's exported GATES manifest reordered into the standard's order: lint, typecheck, check:agents, check:dups, check:deps, check:size, check:debt, check:coverage, check:ci-parity (last) -- moving check:coverage out of the leading position it currently holds. Replace package.json check:all with `bun scripts/check-all.ts`, add `verify`: `bun run check:all`, and add scripts/check-all.test.ts. Confirm the quiet-output contract.","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-134e.\n\nParent seed: trellis-7ffe — Adopt canonical check:all standard\nPlan template: feature\nPlan approach: Swap the && chain for the canonical quiet runner with a GATES manifest in the standard's order (cheap static gates first, check:coverage second-to-last, check:ci-parity last), then add the generalized ci-parity gate and the verify alias.…\n\nRun `sd plan show pl-134e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:53:29.212Z","updatedAt":"2026-06-12T05:45:19.382Z","plan_id":"pl-134e","blocks":["trellis-6f71","trellis-7ffe"],"closedAt":"2026-06-12T05:45:19.382Z"}
{"id":"trellis-6f71","title":"Add scripts/check-ci-parity.ts (with test) copied from templates/l5-toolkit/scripts, importing the GATES array from check-all.ts; add the check:ci-parity script as the final gate in the manifest. Reconcile any residual non-canonical gate name. Verify check:ci-parity passes against .github/workflows/ci.yml.","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-134e.\n\nParent seed: trellis-7ffe — Adopt canonical check:all standard\nPlan template: feature\nPlan approach: Swap the && chain for the canonical quiet runner with a GATES manifest in the standard's order (cheap static gates first, check:coverage second-to-last, check:ci-parity last), then add the generalized ci-parity gate and the verify alias.…\n\nRun `sd plan show pl-134e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:53:29.212Z","updatedAt":"2026-06-12T05:45:19.437Z","plan_id":"pl-134e","blocks":["trellis-4e66","trellis-7ffe"],"closedAt":"2026-06-12T05:45:19.437Z"}
{"id":"trellis-4e66","title":"Wire .github/workflows/ci.yml to invoke the canonical gates (or `bun run check:all`) so the local manifest matches CI. Run `bun run check:all` and `bun run verify` green end-to-end with the quiet-output contract and a passing check:ci-parity.","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-134e.\n\nParent seed: trellis-7ffe — Adopt canonical check:all standard\nPlan template: feature\nPlan approach: Swap the && chain for the canonical quiet runner with a GATES manifest in the standard's order (cheap static gates first, check:coverage second-to-last, check:ci-parity last), then add the generalized ci-parity gate and the verify alias.…\n\nRun `sd plan show pl-134e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:53:29.212Z","updatedAt":"2026-06-12T05:45:19.493Z","plan_id":"pl-134e","blocks":["trellis-7ffe"],"closedAt":"2026-06-12T05:45:19.493Z"}
{"id":"trellis-253e","title":"Replace agent-readiness auditing with deterministic TypeScript sloppiness measurement","status":"open","type":"feature","priority":1,"createdAt":"2026-09-14T21:52:07.505Z","updatedAt":"2026-09-17T04:29:35.169Z","description":"Deliver the approved breaking product pivot: no agent execution or model-derived grading; offline Bun/TypeScript analysis; a versioned 0–100 sloppiness index with separate safeguard results; initial complexity, erosion, duplication, import-cycle and hook/check configuration analysis; local, fleet and CI use through one core. Preserve legacy history separately. Defer project verification execution, unused-code analysis, broader architecture rules and new language adapters. Implementation is governed by the attached forward-chained plan.","plan_id":"pl-b2ea","blockedBy":["trellis-b12d","trellis-d03d"]}
{"id":"trellis-356d","title":"The deterministic product contract replaces the readiness specification","status":"closed","type":"task","priority":1,"plan_step_index":0,"description":"Plan: pl-b2ea / trellis-253e. Step 1 of 23.\n\nRequired state\nRevise SPEC.md and the mission/workflow portions of AGENTS.md and CLAUDE.md to describe the approved breaking pivot. Define the initial metric catalog, separate safeguard results, offline default, unsupported-language coverage, optional history, and local/fleet/CI parity. Define no-model execution as an invariant. Document legacy score separation and the staged implementation transition without claiming unbuilt behavior.\n\nPrerequisites\nNone; this is the plan entry point.\n\nAcceptance criteria\n1. The specification names complexity, erosion, duplication, import cycles, and basic hook/check inspection as release scope.\n2. Unused-code analysis, broader architecture rules, project verification execution, AI features, and new language adapters are explicitly deferred.\n3. The 0–100 lower-is-better index is distinguished from a percentage of bad code; infrastructure cannot offset its value.\n4. The first audit requires neither Git nor credentials, database, network, or installed project dependencies.\n\nForward handoff\ntrellis-ba72, trellis-58a6, trellis-b12d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-15T18:53:18.385Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-ba72","trellis-58a6","trellis-b12d","trellis-253e"],"closedAt":"2026-09-15T18:53:18.385Z","closeReason":"Rewrote SPEC.md as the deterministic product contract: release scope names complexity, erosion, duplication, import cycles, and basic hook/check safeguard inspection; unused-code analysis, broader architecture rules, verification execution, AI features, and new language adapters explicitly deferred; 0-100 lower-is-better index distinguished from a percentage of bad code with infrastructure unable to offset it; first audit requires no Git, credentials, database, network, or installed dependencies. Defined the initial metric catalog (SPEC §5), separate safeguard evidence levels (§5.5), offline default and no-model invariants (§1, §8), unsupported-language coverage (§3.3), optional history with legacy score separation (§10), local/fleet/CI parity (§1, §12), and the staged transition without claiming unbuilt behavior (§14). Revised mission/workflow portions of AGENTS.md and CLAUDE.md to match. bun run check:all green (9/9)."}
{"id":"trellis-ba72","title":"Agent execution is disconnected from every public audit path","status":"closed","type":"task","priority":1,"plan_step_index":1,"description":"Plan: pl-b2ea / trellis-253e. Step 2 of 23.\n\nRequired state\nRemove investigation wiring from src/report/build.ts and run.ts, fleet orchestration/options, CLI flags/environment handling/progress, and SDK request shapes. Remove agent-discovered criteria from the transitional catalog and remove os-eco pass-override wiring. This issue disconnects runtime behavior; deletion of the now-unused subsystem belongs to the next issue.\n\nPrerequisites\ntrellis-356d\n\nAcceptance criteria\n1. CLI audit, fleet, and SDK paths have no route to Pi or another model, including default persistence paths.\n2. Provider/model, piBin, investigation-cache controls and progress events are absent from public configuration and validated options.\n3. Retired agent criteria no longer count as missing measurements or affect the transitional score.\n4. Offline integration tests cover default CLI/SDK execution and legacy configuration rejection with an actionable message.\n\nForward handoff\ntrellis-4abc\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T16:37:11.864Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-4abc","trellis-253e"],"closedAt":"2026-09-16T16:37:11.864Z","closeReason":"Disconnected agent execution from every public audit path (SPEC §14 stage 2). Removed investigation wiring from src/report/build.ts + run.ts (deleted agent-scope.ts), fleet orchestrate/run, CLI audit/fleet flags + TRELLIS_PI_BIN env + progress events, and SDK request shapes; removed the os-eco pass-override fold (build.oseco.test.ts deleted with the wiring). Retired the 20 agent criteria and the all-agent documentation category from the transitional catalog (rubric 0.3.0: 70 deterministic criteria / 8 categories / 8 gates). Legacy config is rejected actionably via new src/legacy.ts: --no-cache, TRELLIS_PI_BIN, targets.yaml defaults.investigation, and retired SDK option keys all fail fast naming the remedy. Offline integration tests cover default CLI/SDK execution and every rejection path. bun run check:all: 9/9 gates green."}
{"id":"trellis-4abc","title":"The investigation subsystem and its cache API are removed","status":"closed","type":"task","priority":1,"plan_step_index":2,"description":"Plan: pl-b2ea / trellis-253e. Step 3 of 23.\n\nRequired state\nDelete src/investigation/, report/agent-scope.ts, live golden capture scripts and fixtures, obsolete exports, and investigation-specific tests and dependencies. Remove cache methods/types/statements from the store without rewriting historical migrations or deleting existing user data. Remove stale live-capture documentation and references.\n\nPrerequisites\ntrellis-ba72\n\nAcceptance criteria\n1. No executable model/provider/agent-grader implementation, prompt, or capture gate remains in shipped source or scripts.\n2. Fresh and existing databases open without investigation cache APIs; historical migrations remain append-only.\n3. Search-based review distinguishes intentionally retained historical release notes from stale executable references.\n4. Quality budgets are updated only according to repository ratchet policy; removals do not justify arbitrary coverage-floor reductions.\n\nForward handoff\ntrellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T17:09:50.688Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T17:09:50.688Z","closeReason":"Deleted src/investigation/ (areas, findings, grader, Pi RPC provider, frozen goldens), scripts/update-pi-golden.ts(+test), and the dead src/detectors/oseco/ overlay with its osecoDetectors toggle (SPEC §14 stage 3). Store cache API (getCache/putCache/CachedFindings) removed from src/store/store.ts+index.ts; migrations untouched and append-only (guarded by a store.test.ts assertion). Stale live-capture docs removed (RUNBOOK §4, SECURITY.md Pi bullet, README/AGENTS/CLAUDE references); report render fixture de-staled and goldens regenerated via the documented gate. CHANGELOG updated; check:all 9/9 green."}
{"id":"trellis-58a6","title":"Versioned measurement, finding, and audit configuration contracts exist","status":"closed","type":"task","priority":1,"plan_step_index":3,"description":"Plan: pl-b2ea / trellis-253e. Step 4 of 23.\n\nRequired state\nIntroduce core types and boundary schemas for metric values with units and numerator/denominator, findings with relative paths and ranges, safeguard evidence, analyzer/scoring/schema versions, source coverage, report metadata and declarative audit configuration. Define complete, incomplete, unsupported and not-applicable states. This issue supplies contracts without implementing analyzers or replacing report consumers.\n\nPrerequisites\ntrellis-356d\n\nAcceptance criteria\n1. Configuration describes source exclusions/classification and analysis policy without executable hooks.\n2. Reports represent raw metrics separately from score contributions and test coverage separately from analysis completeness.\n3. Deterministic measurement payload excludes timestamps and timings from equality/fingerprint inputs.\n4. Schema tests reject invalid ranges, non-finite numbers, missing required version data and misleading complete states.\n\nForward handoff\ntrellis-6003, trellis-ef85, trellis-424d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T19:35:25.710Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-6003","trellis-ef85","trellis-424d","trellis-253e"],"closedAt":"2026-09-16T19:35:25.710Z","closeReason":"Delivered src/contract/: versioned zod contracts for metric values (metric.ts), findings (finding.ts), safeguard evidence (safeguard.ts), source coverage (coverage.ts), the audit report + deterministic measurementPayload (report.ts), declarative hook-free audit configuration (config.ts), analysis states (states.ts), shared primitives (primitives.ts), and pinned ANALYZER/SCORING/SCHEMA versions (version.ts), with 56 schema tests rejecting invalid ranges, non-finite numbers, missing version data, and misleading complete states. No analyzers or consumer rewiring — per the execution boundary. check:all 9/9 green."}
{"id":"trellis-6003","title":"TypeScript source discovery produces a classified workspace inventory","status":"closed","type":"task","priority":1,"plan_step_index":4,"description":"Plan: pl-b2ea / trellis-253e. Step 5 of 23.\n\nRequired state\nImplement TS/TSX file discovery and package/source-set ownership using package manifests and workspace declarations. Classify production, tests, generated, vendored and declaration-only files with documented defaults and explicit overrides. Support uncommitted files and non-Git directories. Reuse discovery helpers only where their app-based assumptions remain valid.\n\nPrerequisites\ntrellis-58a6\n\nAcceptance criteria\n1. Each included file belongs to exactly one scoring source set; nested packages are not double-counted.\n2. Ignored and unsupported source scopes are reported rather than counted as clean.\n3. Temp-directory fixtures cover single packages, nested workspaces, TSX, tests, generated files, exclusions, symlinks and non-Git trees.\n4. No package installation, repository script execution or network lookup occurs.\n\nForward handoff\ntrellis-d81d, trellis-a97d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T19:48:27.387Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-d81d","trellis-a97d","trellis-253e"],"closedAt":"2026-09-16T19:48:27.387Z","closeReason":"src/discovery/ gains discoverSourceInventory (inventory.ts), documented classification defaults + §6.5 overrides (classify.ts), a minimal glob subset (glob.ts), and toSourceCoverage; src/config/load.ts loads+validates trellis.yaml into the §6.5 contract. Nested packages owned by nearest manifest (no double counting); excluded/unsupported/ignored scopes reported as coverage; temp-dir fixtures cover single/nested/TSX/tests/generated/exclusions/symlinks/non-Git; no installs, scripts, or network. 899 tests + all 9 check:all gates green."}
{"id":"trellis-d81d","title":"A shared TypeScript syntax and function inventory is available","status":"closed","type":"task","priority":1,"plan_step_index":5,"description":"Plan: pl-b2ea / trellis-253e. Step 6 of 23.\n\nRequired state\nImplement one parse layer using a pinned TypeScript compiler API dependency with source positions and function ownership. Inventory declarations, expressions, arrows, methods, constructors and accessors. Define handling of overloads, nested functions and source-line counts, including multiline literals and comment-only lines. Expose facts to analyzers without embedding scoring.\n\nPrerequisites\ntrellis-6003\n\nAcceptance criteria\n1. Parsing is reused across metrics within one audit.\n2. Nested function bodies are attributed according to the documented rule and never accidentally counted in parent branch totals.\n3. Parser errors create explicit incomplete analysis with located diagnostics.\n4. Fixtures cover TS/TSX syntax, overload signatures, anonymous callbacks and position mapping.\n\nForward handoff\ntrellis-fbc5, trellis-5a91, trellis-d214\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T20:12:06.167Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-fbc5","trellis-5a91","trellis-d214","trellis-253e"],"closedAt":"2026-09-16T20:12:06.167Z","closeReason":"Delivered src/syntax/: one shared parse layer (pinned typescript 6.0.3, moved to dependencies) with buildSyntaxInventory over the discovery SourceInventory (inventory.ts), per-file parse + located diagnostics (parse.ts), the function inventory with documented naming/overload/nesting rules and walkOwnNodes attribution enforcement (functions.ts), scanner-based line counts with documented multiline-literal/comment-only handling (sloc.ts), and typed facts in types.ts — no scoring. 36 new tests cover TS/TSX, overloads, anonymous callbacks, position mapping, read failures and determinism; bun run check:all passes 9/9."}
{"id":"trellis-fbc5","title":"Complexity and structural erosion measurements are reproducible","status":"closed","type":"task","priority":1,"plan_step_index":6,"description":"Plan: pl-b2ea / trellis-253e. Step 7 of 23.\n\nRequired state\nImplement per-function cyclomatic complexity, maximum nesting, source size, distributions and erosion. Specify exactly which AST decisions count, including logical operators, optional chaining, switch cases, catch clauses and nested functions. Erosion uses mass = CC * sqrt(SLOC) and the share belonging to CC > 10; size/nesting remain explanatory signals unless scoring explicitly includes them.\n\nPrerequisites\ntrellis-d81d\n\nAcceptance criteria\n1. Hand-calculated fixtures verify branch counts, nesting and weighted erosion.\n2. Empty and function-free scopes produce documented finite values or not-applicable states.\n3. Repository/package aggregation uses summed masses rather than averages of package percentages.\n4. Hotspots include exact paths/ranges and production/test measurements remain separate.\n\nForward handoff\ntrellis-00d5, trellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T20:48:52.392Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-00d5","trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T20:48:52.392Z","closeReason":"Delivered src/metrics/ (trellis-fbc5): analyzeComplexity over the shared syntax inventory. complexity.ts pins the exact CC decision table (if/else-if, all loops, case not default, catch, ternaries, &&/||/??/logical-assignments, each ?. token; nested functions opaque) and the nesting rule (if/loops/switch/try only); erosion.ts pins mass = CC*sqrt(SLOC), CC>10 threshold, nearest-rank p50/p90/max; analyze.ts keeps production/test separate, aggregates by summed masses (never averaged shares), emits per-set MetricValues with documented complete/incomplete/not-applicable states and ranked complexity.hotspot findings with exact paths/ranges. src/syntax/sloc.ts gained classifyLines for per-range SLOC. Hand-calculated fixtures verify branch counts, nesting, and weighted erosion (32 new tests); coverage floors ratcheted 78/74 -> 96/96. bun run check:all: 9/9 green."}
{"id":"trellis-5a91","title":"Duplication detection has a bounded, deterministic feasibility decision","status":"closed","type":"task","priority":1,"plan_step_index":7,"description":"Plan: pl-b2ea / trellis-253e. Step 8 of 23.\n\nRequired state\nEvaluate a small token-based implementation versus reuse of an existing embeddable deterministic clone analyzer. Use fixed local fixtures and a declared larger corpus to compare normalized-token behavior, near-clone scope, overlap handling, runtime, memory, dependency and license implications. Record the selected approach and token/line thresholds in SPEC.md; deliver no parallel production implementations.\n\nPrerequisites\ntrellis-d81d\n\nAcceptance criteria\n1. The chosen method runs offline without downloading tools or requiring target dependencies.\n2. The decision fixes exact/normalized clone semantics, minimum clone size, overlap union, and production/test boundary behavior.\n3. Measurements include corpus sizes, environment, timing and limitations rather than unsupported speed claims.\n4. The resulting implementation task has an explicit algorithm/library and expected clone fixture outputs.\n\nForward handoff\ntrellis-6e4c\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T21:05:46.889Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-6e4c","trellis-253e"],"closedAt":"2026-09-16T21:05:46.889Z","closeReason":"Decision recorded in SPEC.md §5.3: own normalized-token detector over the shared parse (zero new deps, type-1+2 semantics, 50-token/3-line provisional minimum, content-identity groups, overlap-union line accounting, per-source-set boundary, budget-guarded incomplete). Evaluation vs jscpd 4.3.0 (JS API) and 5.2.1 (Rust CLI) on 6 hand-authored fixtures + 2 declared corpora (trellis src @853348b; 4x stress) with environment/timing/memory/limitations recorded. Forward design + expected fixture outputs appended to trellis-6e4c. No production implementation shipped."}
{"id":"trellis-6e4c","title":"Duplication metrics identify clone groups and unique affected lines","status":"closed","type":"task","priority":1,"plan_step_index":8,"description":"Plan: pl-b2ea / trellis-253e. Step 9 of 23.\n\nRequired state\nImplement the selected duplication design, using the shared inventory and token facts where appropriate. Emit stable clone groups and ranges, unique duplicated source-line totals and density. Implement bounded resource handling and deterministic ordering; report resource exhaustion as incomplete.\n\nPrerequisites\ntrellis-5a91\n\nAcceptance criteria\n1. Golden or hand-authored fixtures cover exact clones, identifier normalization as selected, overlapping clones, multiple copies and short unrelated matches.\n2. The duplication numerator counts the union of affected lines once and uses a documented compatible denominator.\n3. No clones cross excluded/generated scopes; test-to-production matches follow the contract.\n4. Runtime and memory limits do not silently return a clean result.\n\nForward handoff\ntrellis-00d5, trellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.\n\n---\n\n## Design input from trellis-5a91 (feasibility decision, recorded in SPEC §5.3)\n\n**Algorithm/library**: own normalized-token clone detector over the shared syntax inventory (trellis-d81d) — no new runtime dependency. Per file, walk the shared `ts.SourceFile` and collect leaf tokens in document order (NOT a raw `ts.createScanner` loop: it mis-tokenizes template literals without manual re-scan state — reproduced in the spike). Normalize: every identifier → one placeholder, every literal (string, numeric, bigint, regex, template part) → one placeholder, all other tokens keep their `SyntaxKind`. Hash sliding windows of K=50 normalized tokens; extend equal windows from distinct positions into maximal matches; group by content identity of the maximal normalized sequence (drop same-file token-contained members; no transitive merging). Minimum clone: 50 tokens AND 3 lines (provisional, trellis-e924 calibrates). Detection runs per source set (production and test separately; never cross-set); generated/vendored/declaration-only/excluded files are not tokenized. Numerator = union of code-classified lines inside member ranges counted once per file; denominator = scope code-classified lines. Budgets on token count and match work trip `incomplete`, never a silent clean result.\n\n**Expected clone fixture outputs at K=50** (hand-authored fixtures; spike-verified 2026-09-16):\n- `exact` (identical function in 2 files): 1 group, members a.ts 4-15 + b.ts 4-15; 22 unique duplicated code lines of 27.\n- `renamed` (identifiers + literals renamed, structure identical): 1 group, members a.ts 1-15 + b.ts 1-15; density 1.0. (jscpd 4/5 miss this in default mode.)\n- `overlap` (a.ts = regions X+Y, b.ts = Y+Z, c.ts = X+Y+Z): 2 groups — (a.ts 2-16, c.ts 2-16) and (b.ts 2-16, c.ts 10-24); c.ts's lines counted once in the union (49 unique lines of 49).\n- `multicopy` (same 9-line function in 4 files): 1 group with 4 members (a/b/c/d.ts 1-9).\n- `short` (shared ~30-token idiom): 0 groups at K=50 (below threshold).\n- `near` (2 of 9 lines changed): 1 group covering only the maximal shared run (a.ts 7-13, b.ts 3-9); the divergent tail is not merged (type-3 out of scope).\n\nNote: member line ranges are token-exact maximal runs and may overhang a partial boundary line (e.g. `exact` starts at line 4, the trailing `;`/`}` of a preceding statement) — both jscpd engines behave identically; this is accepted semantics, fixtures should assert it.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T21:32:17.712Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-00d5","trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T21:32:17.712Z","closeReason":"Implemented the trellis-5a91 decision: own normalized-token clone detector over the shared syntax inventory. New src/metrics/duplication.ts (contract + token collector), duplication-detect.ts (rolling-hash windows, maximal extension, content-identity grouping, latching match-work budget), duplication-groups.ts (min-line/containment/subsumption finalization, deterministic ids), analyze-duplication.ts (per-source-set scopes, union numerator over code-line denominator, duplication.groups/duplicated-lines/density metrics, clone-group findings, budget-exhaustion incomplete states). 28 new tests pin exact/renamed/overlap/multicopy/short/near fixtures, scope discipline (no cross-set or generated/vendored/declaration-only/excluded clones), and both budget trips; SPEC §5.3 records the landed budgets; CHANGELOG updated. check:all 9/9 green."}
{"id":"trellis-d214","title":"Workspace-aware import resolution produces a documented dependency graph","status":"closed","type":"task","priority":1,"plan_step_index":9,"description":"Plan: pl-b2ea / trellis-253e. Step 10 of 23.\n\nRequired state\nReplace the regex/relative-only graph foundation with AST imports and TypeScript resolution appropriate to tsconfig aliases and local workspace packages. Record type-only versus runtime edges, re-exports, literal dynamic imports and unresolved imports. Resolve through local files/configuration only; document behavior when dependencies are absent.\n\nPrerequisites\ntrellis-d81d\n\nAcceptance criteria\n1. Fixtures cover aliases, package exports used by supported resolution, extension mapping, barrels, workspace boundaries and dynamic imports.\n2. External packages are distinguishable from unresolved local edges; incompleteness is surfaced.\n3. Type-only edges retain their identity and graph policies are versioned.\n4. Comments and string contents cannot forge imports.\n\nForward handoff\ntrellis-cbde\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T21:51:45.959Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-cbde","trellis-253e"],"closedAt":"2026-09-16T21:51:45.959Z","closeReason":"Delivered workspace-aware import resolution + documented dependency graph in src/metrics/: graph-imports.ts (AST extraction — comments/strings/JSDoc cannot forge edges; import/re-export/dynamic kinds; type-only identity incl. type-position import types; non-literal dynamic recorded), graph-resolve.ts (pinned-compiler resolution over local files/config only — guarded host never consults node_modules; nearest-tsconfig paths/baseUrl; extension mapping; out-of-scope vs local targets), graph-workspace.ts (pure exports/main/types/index machinery with encapsulation), graph-types.ts (versioned GRAPH_POLICY 1.0.0), analyze-graph.ts (analyzeDependencyGraph → DependencyGraph + graph.files/edges.local/edges.external/edges.unresolved metrics + graph.unresolved-import findings). 45 new tests pin aliases, package exports, extension mapping, barrels, workspace boundaries, dynamic imports, forgery resistance, states, determinism. SPEC §5.4 + CHANGELOG updated; check:all 9/9 green; dogfooded over trellis src: 216 nodes / 530 local / 333 external / 0 unresolved, complete, ~270ms."}
{"id":"trellis-cbde","title":"Import-cycle measurements expose complete cycle groups","status":"closed","type":"task","priority":1,"plan_step_index":10,"description":"Plan: pl-b2ea / trellis-253e. Step 11 of 23.\n\nRequired state\nImplement strongly connected components over the resolved graph and report cyclic module groups, affected-module density and representative paths. Define whether runtime and type-only cycles are scored separately. Replace the first-cycle-only behavior without adding architecture boundary rules.\n\nPrerequisites\ntrellis-d214\n\nAcceptance criteria\n1. Multiple disjoint and overlapping cycles, acyclic graphs and self-imports have specified tested outcomes.\n2. Group identifiers and representative paths are stable across filesystem enumeration order.\n3. Package and repo views preserve cross-package cycles without double-counting.\n4. Unresolved graph coverage accompanies cycle results.\n\nForward handoff\ntrellis-00d5, trellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T22:06:01.645Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-00d5","trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T22:06:01.645Z","closeReason":"Delivered complete cycle-group measurement: src/metrics/cycles.ts (iterative Tarjan SCC, stable representative paths, CYCLE_POLICY 1.0.0 defining runtime/type-only classes scored separately, self-edge = size-1 group) + src/metrics/analyze-cycles.ts (analyzeCycles consuming the trellis-d214 graph: cycle-<n> groups, per-package views without double-counting, import-cycle.groups/modules/density metrics, located import-cycle findings, graph incompleteness rolled up per SPEC 3.3). SPEC 5.4 landed annotation, CHANGELOG entry, 21 new tests covering disjoint/overlapping/acyclic/self-import/type-only/cross-package/unresolved/determinism cases. bun run check:all: 9/9 gates green."}
{"id":"trellis-a97d","title":"Safeguards are inspected as configuration with explicit evidence levels","status":"closed","type":"task","priority":1,"plan_step_index":11,"description":"Plan: pl-b2ea / trellis-253e. Step 12 of 23.\n\nRequired state\nAdapt useful common/TypeScript detectors into basic configuration inspection for Git pre-commit hooks, supported agent hooks, lint/typecheck/test scripts, coverage/size/duplication budgets and references to checks. Define a small documented set of supported formats. Inspect paths and recognizable command references without running hooks, importing executable configs or claiming arbitrary shell behavior is proven.\n\nPrerequisites\ntrellis-6003\n\nAcceptance criteria\n1. Results distinguish absent, configured, structurally wired, and unknown; passing execution is never inferred.\n2. Broken local hook/check references produce located findings.\n3. Custom named scripts can be recognized through supported wiring rather than named-tool dependency presence alone.\n4. Unsupported shell constructs or executable configuration remain explicitly unverified.\n5. No use of seeds/mulch/canopy or presence of agent instructions grants structural score credit.\n\nForward handoff\ntrellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T22:33:03.064Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T22:33:03.064Z","closeReason":"Delivered src/safeguards/ — deterministic, configuration-only safeguard inspection (SPEC §5.5). inspectSafeguards(root) reports 8 safeguards (pre-commit-hook, agent-hooks, lint/typecheck/test-script, coverage/file-size/duplication-budget) at explicit evidence levels absent/configured/structurally-wired/unknown, over a documented supported-format set (root package.json scripts with line locations, GitHub Actions run:/uses: subset, core.hooksPath/husky/lefthook/pre-commit-framework hook configs, Claude Code .claude/settings.json hooks, JSON budget files + .jscpd.json). Broken local hook/check references produce located safeguard.broken-reference findings; custom-named scripts are recognized via command bodies + transitive run-reference wiring to CI (never dependency presence); executable configs/runners and arbitrary shell stay explicitly unverified (unknown/notes); seeds/mulch/agent instructions grant nothing. 65 tests incl. schema validation + determinism; all 9 check:all gates green; coverage floor ratcheted 96→97 functions. Files: src/safeguards/{types,context,shell,wiring,hooks,agent-hooks,scripts,budgets,inspect,index}.ts + tests."}
{"id":"trellis-00d5","title":"The provisional sloppiness formula is explicit and versioned","status":"closed","type":"task","priority":1,"plan_step_index":12,"description":"Plan: pl-b2ea / trellis-253e. Step 13 of 23.\n\nRequired state\nImplement pure scoring over structural raw metrics and document normalization thresholds and weights. Group overlapping complexity/erosion/size signals to prevent accidental multiple penalties; safeguards and test code do not offset production debt. Include contribution explanations, missing-analysis policy and package aggregation. Label the initial formula provisional pending calibration.\n\nPrerequisites\ntrellis-fbc5, trellis-6e4c, trellis-cbde\n\nAcceptance criteria\n1. Fixtures prove bounds, lower-is-better monotonicity, stable rounding and contribution totals.\n2. Missing required dimensions prevent publication of an apparently complete headline score.\n3. Large clean additions cannot erase hotspot findings; counts and densities are both retained.\n4. Default formulas are consistent across repos; policy budgets do not silently mutate scoring weights.\n5. Each contribution is traceable to raw metrics and findings.\n\nForward handoff\ntrellis-ef85\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T22:47:44.914Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-ef85","trellis-253e"],"closedAt":"2026-09-16T22:47:44.914Z","closeReason":"Provisional sloppiness formula landed, explicit and versioned: SCORING_FORMULA pins all constants under SCORING_VERSION 0.1.0-provisional (src/scoring/formula.ts); scoreSloppiness is a pure function of raw contract metrics (src/scoring/sloppiness.ts) with grouped complexity-erosion dimension (weights 0.50/0.30/0.20), per-term saturation thresholds, count+density terms so clean additions never dilute hotspots, production-only scoring, missing-analysis worst-case + partial flag, round-half-up index and largest-remainder contribution apportionment summing exactly to the index, per-dimension trace explanations plus the §6.4 contract score view. Documented in SPEC §7.1; CHANGELOG entry; 27 new tests prove bounds, monotonicity, stable rounding, contribution totals, missing-analysis policy, count retention, config independence, and summed-mass aggregation (100% coverage on both files; all 9 check:all gates green)."}
{"id":"trellis-ef85","title":"The deterministic audit core assembles the new report","status":"closed","type":"task","priority":1,"plan_step_index":13,"description":"Plan: pl-b2ea / trellis-253e. Step 14 of 23.\n\nRequired state\nReplace rubric-driven audit orchestration with inventory -> shared analysis -> metrics/safeguards -> scoring -> report. Remove broad readiness categories, os-eco scoring overlays and retired Python/Swift detectors from active execution. Preserve thin core/service boundaries and bounded progress events. Keep persistence and policy evaluation outside the measurement pass.\n\nPrerequisites\ntrellis-4abc, trellis-58a6, trellis-fbc5, trellis-6e4c, trellis-cbde, trellis-a97d, trellis-00d5\n\nAcceptance criteria\n1. One core call audits TS/TSX locally with no model, network, project command or database access.\n2. Same files/config/analyzer versions yield equal measurement payloads; dirty worktrees are analyzed as they exist.\n3. Unsupported languages and partial analyzer failures produce accurate coverage and useful remaining findings.\n4. End-to-end fixtures exercise a complete and incomplete audit without filesystem mocks.\n\nForward handoff\ntrellis-a059, trellis-942c, trellis-424d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T23:05:34.800Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-a059","trellis-942c","trellis-424d","trellis-253e"],"closedAt":"2026-09-16T23:05:34.800Z","closeReason":"Delivered the deterministic audit core in src/audit/: audit.ts (auditWorkspace — one core call: configure → discover → shared parse → complexity/duplication/graph/cycles → safeguards → pure score → §6.4 assembly; no model/network/project-command/Git/DB access, zero footprint), assemble.ts (pure, schema-validated report assembly enforcing the §6.4 honesty invariants; sorted metrics, kind-grouped findings, sloc-filled coverage), progress.ts (events bounded by pipeline shape). E2E fixtures in audit.test.ts cover complete/incomplete audits, determinism (byte-equal measurement payloads), dirty worktrees, unsupported-language coverage, and budget exhaustion over real temp dirs. Also fixed analyze-duplication emitting denominator 0 for code-free scopes (§6.1 violation the core's validation surfaced). Gates: bun run check:all 9/9 green."}
{"id":"trellis-a059","title":"Terminal, JSON and Markdown render the new metric report","status":"closed","type":"task","priority":1,"plan_step_index":14,"description":"Plan: pl-b2ea / trellis-253e. Step 15 of 23.\n\nRequired state\nReplace readiness scorecards with concise headline score/completeness, raw metric summaries, score contributions, ranked hotspots and separate safeguard evidence. Keep presentation in renderers; produce full structured findings in JSON and useful bounded summaries in terminal/Markdown.\n\nPrerequisites\ntrellis-ef85\n\nAcceptance criteria\n1. Render fixtures cover clean, sloppy, mixed-language, incomplete and function-free repositories.\n2. Every displayed score has its direction and scoring version; output never implies a percentage of bad code.\n3. Hotspots point to usable relative paths and line locations.\n4. No readiness levels, agent progress or legacy category labels appear in new reports.\n\nForward handoff\ntrellis-9a88\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T23:26:04.288Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-9a88","trellis-253e"],"closedAt":"2026-09-16T23:26:04.288Z","closeReason":"Terminal, JSON and Markdown renderers for the §6.4 metric report: src/report/audit-terminal.ts (bounded human summary: headline index with direction + scoring version, coverage, traceable contributions, raw metrics, ranked hotspots, safeguard evidence), src/report/audit-json.ts (full structured document, re-validated at the boundary), src/report/audit-markdown.ts (bounded summary), shared presentation helpers in src/report/audit-format.ts, and src/report/audit-fixtures.ts covering clean/sloppy/mixed-language/incomplete/function-free repositories audited through the real core. Tests beside each unit; SPEC §12 annotated; coverage floor ratcheted 96→97 lines. bun run check:all 9/9 green."}
{"id":"trellis-942c","title":"Baseline comparison and failure policies operate on report artifacts","status":"closed","type":"task","priority":1,"plan_step_index":15,"description":"Plan: pl-b2ea / trellis-253e. Step 16 of 23.\n\nRequired state\nImplement core comparison of two validated new-format reports and independent policy assessment for metric budgets, score regressions and new cycle/hotspot findings. Document absolute versus relative tolerances and conservative finding matching. Require compatible analyzer/scoring/configuration/source-scope semantics, and report incompatible comparisons explicitly.\n\nPrerequisites\ntrellis-ef85\n\nAcceptance criteria\n1. Saved JSON reports compare without Git or SQLite.\n2. Tests cover new/resolved/persistent findings, line shifts, changed exclusions, analyzer version differences and incomplete reports.\n3. A better aggregate score cannot suppress an independently configured cycle or hotspot policy failure.\n4. Policies return structured reasons and preserve the existing success/operational-error/policy-failure distinction.\n\nForward handoff\ntrellis-424d, trellis-9a88\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-16T23:46:58.655Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-424d","trellis-9a88","trellis-253e"],"closedAt":"2026-09-16T23:46:58.655Z","closeReason":"Delivered src/compare/: load.ts (saved JSON report artifacts load + §6.4 re-validation, no Git/SQLite, operational ReportArtifactError), compare.ts (pure artifact comparison with explicit compatibility gate over schema/analyzer/scoring versions, metric catalog and supplied source configs; caveats for unverifiable config and changed source scope; conservative kind+path finding matching with line-shift tolerance and ambiguous n:m → resolved+new pairs), policy.ts (independent max-index, metric-budget, score-regression [absolute maxIncrease / relative maxIncreasePercent tolerances] and failOnNew assessment with structured coded reasons; better index cannot suppress cycle/hotspot failures; absent baseline skips, incompatible baseline fails closed). Extended src/contract/config.ts with the regression policy knob; SPEC §6.5/§9 updated. Tests cover new/resolved/persistent findings, line shifts, changed exclusions, version differences, incomplete reports, and saved-artifact comparison via real audited fixtures. bun run check:all: 9/9 gates green."}
{"id":"trellis-424d","title":"History preserves legacy runs and stores new measurements separately","status":"closed","type":"task","priority":1,"plan_step_index":16,"description":"Plan: pl-b2ea / trellis-253e. Step 17 of 23.\n\nRequired state\nAdd append-only SQLite migration and typed store operations for new report versions, repository identity and compatible trend queries. Preserve old runs as legacy readiness history without comparing their numeric values with sloppiness. Remove any remaining live investigation-cache coupling; keep new audits usable without persistence.\n\nPrerequisites\ntrellis-58a6, trellis-ef85, trellis-942c\n\nAcceptance criteria\n1. A populated legacy database migrates without losing original report records.\n2. Legacy and new runs are visibly distinct and never form a mixed score trend.\n3. Fresh DB, reopen, round-trip, prior-compatible-run selection and disabled persistence tests pass against real SQLite.\n4. Repository identity avoids accidental collisions between unrelated directories with the same basename.\n\nForward handoff\ntrellis-9a88\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T00:11:54.640Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-9a88","trellis-253e"],"closedAt":"2026-09-17T00:11:54.640Z","closeReason":"Delivered SPEC §10 history: migration 0002 (src/store/migrations/0002-audit-runs.sql) adds append-only audit_runs beside untouched legacy tables; src/store/audit-store.ts adds the composed AuditStore (insertAuditRun, latestAuditRun, auditRuns, compatibleAuditRuns, latestCompatibleRun, sloppinessTrend, auditRepos), repoIdentity (label#sha256(canonical root)) and storedAuditReport; src/store/store.ts composes it into Store and tags legacy rows kind=legacy-readiness vs kind=sloppiness so trends never mix. Tests in src/store/audit-store.test.ts + repo-identity.test.ts cover fresh DB, populated-legacy migration, reopen, round-trip, prior-compatible-run selection, same-basename collision avoidance, and disabled persistence against real SQLite/temp dirs. No live investigation-cache coupling remains (table is append-only history with no API). bun run check:all: 9/9 green."}
{"id":"trellis-9a88","title":"CLI and SDK expose the same simplified deterministic audit","status":"closed","type":"task","priority":1,"plan_step_index":17,"description":"Plan: pl-b2ea / trellis-253e. Step 18 of 23.\n\nRequired state\nWire the approved audit/report/config surface to the new core and schemas. Preserve terminal/JSON/Markdown output and 0/1/2 exit conventions; expose optional baseline, policy, output and history controls. Remove old rubric/level/provider flags and give useful errors for obsolete options. Make audit without explicit persistence stateless by default.\n\nPrerequisites\ntrellis-a059, trellis-942c, trellis-424d\n\nAcceptance criteria\n1. CLI and SDK deep-equal tests prove one measurement and policy code path.\n2. A non-Git temp project can be audited without credentials or preinstalled project tools.\n3. Default audit creates no hidden database or report files; requested outputs and persistence work.\n4. Policy failure still emits the report, while operational failures are distinct.\n5. Help text accurately describes new commands, defaults and retired features.\n\nForward handoff\ntrellis-8366, trellis-e924\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T01:43:56.547Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-8366","trellis-e924","trellis-253e"],"closedAt":"2026-09-17T01:43:56.547Z","closeReason":"CLI and SDK now expose the same deterministic audit (SPEC §12). trellis audit folds runWorkspaceAudit (src/audit/run.ts): --config/trellis.yaml → deterministic core → --baseline or history-resolved baseline → declarative policy (src/compare/policy.ts) → opt-in --history persistence; stateless by default (no DB, no report files unless --out/--history). New trellis compare folds runComparison (src/compare/run.ts) with renderers in src/report/compare-render.ts; incompatible pairs fail closed (exit 2). Retired flags (--rubric-version, --min-level, --fail-on, --canonical, --no-persist, --output, --no-cache, TRELLIS_PI_BIN) fail fast with actionable pivot errors. SDK audit/compare (src/client/index.ts) call the same services; deep-equal parity tests cover measurement and policy (src/client/index.test.ts). Acceptance: non-Git temp audits, stateless default, exit 2 policy vs exit 1 operational, and help-text accuracy all tested (src/cli/audit.test.ts, compare.test.ts, exit-codes.test.ts). check:all 9/9 green."}
{"id":"trellis-8366","title":"Fleet and standards remain optional consumers of the new core","status":"closed","type":"task","priority":1,"plan_step_index":18,"description":"Plan: pl-b2ea / trellis-253e. Step 19 of 23.\n\nRequired state\nAdapt targets configuration, fleet aggregation and history views to new reports and policy results. Remove investigation defaults, readiness skips and maturity comparisons with migration guidance. Retain canonical drift/standards as separate capabilities that do not contribute to sloppiness. Do not expand standards or introduce scheduling/hosting.\n\nPrerequisites\ntrellis-9a88\n\nAcceptance criteria\n1. Two-repository fixtures cover successful audits, a failed target, policy failure and compatible aggregate summaries.\n2. Fleet results match independent core audits and preserve per-repository findings/completeness.\n3. Canonical drift remains available independently and cannot change the structural score.\n4. Legacy targets receive actionable validation errors for removed configuration.\n\nForward handoff\ntrellis-7203\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T02:23:30.707Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-7203","trellis-253e"],"closedAt":"2026-09-17T02:23:30.707Z","closeReason":"Fleet and standards are now optional consumers of the deterministic core (SPEC §10–§11). src/fleet/: pivoted targets.yaml schema (id/path/config/canonical; retired skip/languages/defaults.investigation rejected with actionable migration errors), orchestration folding runWorkspaceAudit per target so fleet results are deep-equal to independent core audits with full findings/completeness preserved, per-target canonical drift as a separate non-scoring capability (isolated driftError, never gates), declarative-policy exit rollup (assessFleet), new terminal/markdown renderers, and stateless-by-default runs with opt-in --history baselines. src/history/: sloppiness dashboard (snapshot + §3.5-compatible index series) with legacy readiness runs in a visibly distinct never-compared section. CLI fleet/report rewritten (retired --fail-on/--min-level/--no-cache fail actionably), SDK fleet()/report() updated with deep-equal CLI parity test, targets.yaml.example/AGENTS.md/SPEC.md/CHANGELOG.md updated. Two-repository fixtures cover successful audits, a failed target, policy failure, and aggregate summaries; bun run check:all is 9/9 green."}
{"id":"trellis-e924","title":"A fixed TypeScript corpus validates score behavior and performance","status":"closed","type":"task","priority":1,"plan_step_index":19,"description":"Plan: pl-b2ea / trellis-253e. Step 20 of 23.\n\nRequired state\nRun the new analyzer against trellis and a small reproducible set of representative TS fixtures/checkouts, recording revisions, source sizes, machine and analyzer versions. Exercise paired refactors such as clone removal, branch growth and cycle introduction. Calibrate provisional normalization if evidence requires it, updating its version and test expectations together.\n\nPrerequisites\ntrellis-9a88\n\nAcceptance criteria\n1. The record includes runtime and peak-memory observations with explicit target budgets chosen from the measured corpus.\n2. Controlled paired changes move their intended metrics predictably and leave unrelated metrics explainable.\n3. Score dilution, small-repo behavior, test separation and incomplete-analysis handling receive explicit review.\n4. Only necessary performance fixes are included; larger algorithm work is filed as a separate blocker if needed.\n5. No model judgments or network access occur during an audit; external corpus acquisition is a separate preparation step.\n\nForward handoff\ntrellis-7203\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T04:03:26.184Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-7203","trellis-253e"],"closedAt":"2026-09-17T04:03:26.184Z","closeReason":"Delivered the fixed TypeScript corpus and validation record: corpus/ (11 committed fixture workspaces + trellis-self, manifest.json with explicit runtime/peak-memory budgets, review checks, and paired-refactor expectations; README.md), scripts/validate-corpus.ts + scripts/corpus-report.ts harness (child-process VmHWM measurement, budget enforcement, pair/check assessment) with colocated tests, and docs/corpus-validation.md (environment/revisions/sizes, observations vs budgets, paired results for clone removal / branch growth / cycle introduction / dilution, explicit reviews of score dilution, small-repo behavior, test separation, and incomplete-analysis handling). Calibration: DUPLICATION_MIN_TOKENS 50→100 (SPEC §5.3, analyzer version 0.1.0→0.2.0), scoring constants unchanged (stays 0.1.0-provisional), DEFAULT_DUPLICATION_BUDGET confirmed. No performance fixes were needed; no larger algorithm work filed (one unrelated flaky-test issue filed as trellis-87d9). No model or network access in any audit path; bun run check:all 9/9 green."}
{"id":"trellis-7203","title":"Release documentation and portable usage examples reflect the pivot","status":"closed","type":"task","priority":1,"plan_step_index":20,"description":"Plan: pl-b2ea / trellis-253e. Step 21 of 23.\n\nRequired state\nUpdate README, CHANGELOG, RUNBOOK, package metadata and architecture documentation for the breaking release. Include local refactor review, saved-report comparison, optional fleet/history and generic scheduled/pre-release examples using the same CLI. Document metric definitions, known limitations, unsupported languages and migration from readiness reports/configuration.\n\nPrerequisites\ntrellis-8366, trellis-e924\n\nAcceptance criteria\n1. Examples use implemented flags and work without a hosted service.\n2. CI examples pin analyzer versions, retain artifacts and show policy failure separately from operational error.\n3. Install/package smoke test confirms rubric retirement does not omit required analyzer assets/dependencies.\n4. No Rust rewrite, web UI, automatic remediation or verification-execution feature is implied.\n\nForward handoff\ntrellis-d03d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T04:29:35.169Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-d03d","trellis-253e"],"closedAt":"2026-09-17T04:29:35.169Z","closeReason":"Delivered the release-docs stage: README.md rewritten for the deterministic sloppiness audit (implemented flags, offline examples: refactor review via --out/--baseline/compare, fleet/history, CI gate pinning @os-eco/trellis-cli@0.2.0 with upload-artifact retention and exit-2-policy vs exit-1-operational branching; metric catalog, formula weights, known limitations, unsupported-language coverage, readiness migration); docs/architecture.mmd and CLAUDE.md module graph refreshed; package.json description/keywords pivoted + smoke:package script; RUNBOOK.md wires the smoke test into release gate, post-publish smoke and checklist; CHANGELOG.md Unreleased entry; new scripts/smoke-package.ts + test proves the packed tarball ships all analyzer assets/deps and audits a fixture (acceptance criterion 3). bun run check:all: 9/9 gates green."}
{"id":"trellis-b12d","title":"The legacy backlog is reconciled with the approved release scope","status":"open","type":"task","priority":1,"plan_step_index":21,"description":"Plan: pl-b2ea / trellis-253e. Step 22 of 23.\n\nRequired state\nReview existing open Seeds items and document their relationship to trellis-253e. Close only clearly superseded readiness/agent work with explicit reasons; retain legitimate bugs and explicitly defer execution-tier, automatic issue filing and standards-expansion features. Link relevant historical false-negative/variance learnings to the new issues without adopting oversized legacy tasks into this plan.\n\nPrerequisites\ntrellis-356d\n\nAcceptance criteria\n1. Every currently open legacy feature/bug has a documented keep, superseded, or deferred disposition.\n2. No unrelated completed plan or issue is silently reopened or deleted.\n3. trellis-589a, trellis-98c0 and trellis-7bae are evaluated against replacement behavior/tests.\n4. trellis-14df, trellis-788c, trellis-8d8a, trellis-d02e, trellis-79c9 and trellis-9aa1 do not silently expand this release.\n\nForward handoff\ntrellis-d03d\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-15T18:53:18.385Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blocks":["trellis-d03d","trellis-253e"]}
{"id":"trellis-d03d","title":"The breaking release passes complete offline acceptance and quality gates","status":"open","type":"task","priority":1,"plan_step_index":22,"description":"Plan: pl-b2ea / trellis-253e. Step 23 of 23.\n\nRequired state\nValidate the completed product against the feature acceptance contract, run the required repository gates, and finish targeted regression coverage for public-path agent removal and offline default behavior. Review the shipped tree and migration examples, reconcile completed child issues, and record implementation insights. This issue is final integration verification, not a bucket for missing feature implementation.\n\nPrerequisites\ntrellis-7203, trellis-b12d\n\nAcceptance criteria\n1. bun run lint, bun run typecheck, bun test and bun run check:all all pass without loosening ratchets outside policy.\n2. CLI/SDK/fleet policy parity, no-Git audit, artifact comparison, optional history migration and separate standards drift pass integration checks.\n3. No model/provider configuration or executable agent investigation is shipped.\n4. Trellis audits itself under the new metrics and the calibration record explains its score; obsolete L4+ acceptance is retired.\n5. Remaining material release blockers receive separate scoped issues; commits stay local unless the user requests a push.\n\nForward handoff\nNone; final plan acceptance.\n\nExecution boundary\nThis issue is one agent work unit. Implement only the stated deliverable; downstream integration belongs to the named consumers. Use real temporary directories/SQLite for tests and stub only true external boundaries. For code changes, run bun run lint, bun run typecheck, bun test and bun run check:all before commit. Respect ratchets, record material insights, and keep commits local unless the user requests a push. Planning/documentation-only work validates references and Seeds integrity. Any newly discovered work that exceeds this boundary receives a separately scoped dependency issue.","createdAt":"2026-09-14T21:54:20.560Z","updatedAt":"2026-09-17T04:29:35.169Z","labels":["deterministic-pivot"],"plan_id":"pl-b2ea","blockedBy":["trellis-b12d"],"blocks":["trellis-253e"]}
{"id":"trellis-5fb5","title":"Quality gates complete in the restricted local environment","status":"open","type":"bug","priority":2,"createdAt":"2026-09-14T21:55:10.100Z","updatedAt":"2026-09-14T21:55:10.100Z","description":"Observed while creating plan pl-b2ea on 2026-09-14, with no application source changes: bun run check:all fails check:dups because bun cannot write to tempdir (EPERM), and check:coverage because hasAgentTrailers > detects a Co-authored-by agent trailer in git history fails. All other gates pass. Scope: reproduce these two failures, determine environment versus fixture causes, and restore or document a supported gate invocation without loosening budgets. The agent-trailer detector may be retired by the deterministic pivot; resolve this issue accordingly when its coverage disappears. Acceptance: focused reproduction explains each cause and the supported full gate invocation passes, or the now-obsolete test is explicitly removed by the pivot.","labels":["validation"]}
{"id":"trellis-87d9","title":"CLI report test flakes under full-suite parallel load","status":"open","type":"bug","priority":2,"createdAt":"2026-09-17T04:01:47.680Z","updatedAt":"2026-09-17T04:01:47.680Z","description":"Observed during trellis-e924: 'src/cli/report.test.ts > trellis report > the --repo filter narrows the dashboard to one identity' failed once under 'bun run check:coverage' (full suite, ~5s into the test, expect(...).toBe(...) mismatch) and passed standalone and in three subsequent full-suite runs. The test spawns 'bun run src/cli/main.ts audit --history' subprocesses; under parallel load the audit child presumably returned a non-zero exit or raced the store. Investigate whether the subprocess audit needs a timeout/retry or the test needs isolation; the flake predates or is orthogonal to the corpus work (that path was untouched)."}
{"id":"trellis-2d45","title":"Spike structural slop hypotheses with paired examples and self-audit","status":"closed","type":"task","priority":2,"createdAt":"2026-09-17T02:41:10.518Z","updatedAt":"2026-09-17T02:45:09.705Z","closedAt":"2026-09-17T02:45:09.705Z"}
{"id":"trellis-f999","title":"Validate repeated-decision and abstraction hypotheses against maintenance tasks","status":"open","type":"task","priority":2,"createdAt":"2026-09-17T02:44:51.277Z","updatedAt":"2026-09-17T02:44:51.277Z","description":"Follow up docs/research/slop-spike.md (trellis-2d45). Build held-out TypeScript maintenance tasks with independent review; include legitimate facades, callbacks, transaction wrappers, unrelated dispatch domains, default/subset behavior, if-chain and lookup-table transformations. Compare signals with LOC, CC and duplication. Report false positives and misses before proposing scoring changes. Raw forwarding counts are rejected as a slop score; the self-audit produced 25 mostly legitimate sites, one repeated disposition-tally family and zero near-match disagreements."}
{"id":"trellis-f6b0","title":"Path-mapped non-TS asset imports (e.g. '@/index.css') are reported unresolved and degrade import-cycle scoring to worst case","status":"open","type":"bug","priority":1,"createdAt":"2026-09-17T02:45:00.634Z","updatedAt":"2026-09-17T02:45:00.634Z","description":"Observed auditing warren (2026-09-16): src/ui/src/main.tsx:4 imports '@/index.css'. The file exists at src/ui/src/index.css, but the audit emits graph.unresolved-import ('matches a tsconfig path mapping but resolved to no file').\n\nRoot cause: src/metrics/graph-resolve.ts resolveEdge() asks the TS module resolver, which only resolves TS/JS/JSON extensions. A path-mapped specifier that names a non-TS asset (.css, .svg, .png, ...) returns null, then matchesPathPattern() hits and the edge is classed unresolved/no-target.\n\nImpact: one such edge marks import-cycle.density / .groups / .modules incomplete. sloppiness.ts scores an incomplete metric at full weight, so the import-cycle dimension jumped from ~12.8 raw points to 20, and the report became PARTIAL / completeness: incomplete. A Vite app's stylesheet import is standard, so every Vite/React repo hits this.\n\nRequired state: when the path mapping substitutes to a file that exists on disk but is not an analyzable source, classify the edge as a resolved non-source asset (not a graph node, not unresolved) so it does not make the graph incomplete. A mapping that substitutes to no existing file stays unresolved.\n\nAcceptance criteria\n1. A fixture with a tsconfig paths alias and an aliased .css import produces no graph.unresolved-import finding and graph.edges.unresolved is 0.\n2. An aliased import whose target file does not exist is still reported unresolved.\n3. Relative asset imports ('./index.css') keep their current behavior; confirm they are already handled and add a test if not.","labels":["graph","false-positive"]}
{"id":"trellis-b412","title":"coverage-budget and file-size-budget safeguards miss enforcement when the check script reads the budget path internally","status":"open","type":"bug","priority":2,"createdAt":"2026-09-17T02:45:00.680Z","updatedAt":"2026-09-17T02:45:00.680Z","description":"Observed auditing warren (2026-09-16): coverage-budget and file-size-budget both report 'configured · no CI-reachable check references the budget'. Both are enforced in CI: .github/workflows/ci.yml runs 'bun run check:size' and 'bun run check:coverage:ci'; package.json maps those to 'bun run scripts/check-file-sizes.ts' and 'bun run scripts/check-coverage.ts --junit'; each script resolves its budget file internally (BUDGETS_PATH = resolve(REPO_ROOT, 'scripts/...-budgets.json')).\n\nRoot cause: src/safeguards/budgets.ts BUDGET_SPECS uses enforcedBy: body.includes(budgetPath) for both kinds, so enforcement is only seen when the budget path appears literally in a package.json script body or a workflow command. This is the l5-toolkit layout, so every os-eco fleet repo is under-reported the same way. duplication-budget escapes because it matches the jscpd tool name instead.\n\nRequired state: follow one hop from a CI-reachable script body to the local script file it runs (extractLocalPaths already finds it) and treat a literal reference to the budget path inside that file as enforcement. Read the file as text only; never import or execute it. Note the evidence chain in the surface notes (script -> file -> budget).\n\nAcceptance criteria\n1. A fixture with the l5-toolkit layout (CI -> package script -> scripts/check-*.ts naming the budget JSON) reports structurally-wired for both budgets.\n2. A budget file that no CI-reachable script or script file names stays 'configured'.\n3. A script file that names the budget but is not CI-reachable stays 'configured'.\n4. Safeguards remain outside the score.","labels":["safeguards","false-negative"]}
{"id":"trellis-831b","title":"Calibration evidence: absolute-count saturation thresholds pin large repos at a 50-point floor (warren scores 100/100)","status":"open","type":"task","priority":1,"createdAt":"2026-09-17T02:45:00.727Z","updatedAt":"2026-09-17T02:45:00.727Z","description":"Input for trellis-e924 (corpus calibration). Data point from auditing warren at scoring 0.1.0-provisional, 2026-09-16: 894 production files, 121519 sloc, 7375 functions -> index 100/100.\n\nTerm trace (value / saturatesAt):\n- erosion.eroded-count.production 263 / 20 (13x over)\n- erosion.eroded-share.production 0.3041 / 0.25\n- duplication.density.production 0.2059 / 0.15\n- duplication.groups.production 1381 / 15 (92x over)\n- import-cycle.density 0.0278 / 0.10 (27.8 normalized)\n- import-cycle.groups 10 / 5\nRaw total 92.8; reported 100 because import-cycle was degraded by an unresolved-import false positive (filed separately).\n\nProblem: the count terms (20 eroded functions, 15 clone groups, 5 cycle groups) are repo-size-blind and saturate at values any repo past roughly 10k sloc exceeds. They carry half of every dimension, so every large repo has a floor of 50 regardless of density, and the index has no resolution in the range where real repos live. Once saturated, the term gives zero gradient: warren could remove 1300 clone groups and the term would not move. SPEC 3.4 wants counts retained so clean additions cannot dilute debt; that intent survives a threshold that is reachable, but not one that is always exceeded.\n\nSecond observation: duplication.density.production is 20.6 percent here while warren's own jscpd gate passes at a low single-digit threshold. Record the detector settings (min tokens/lines, whether type-only and import blocks count) beside the corpus results so the density threshold is calibrated against what this detector measures, not against jscpd intuition.\n\nOptions to evaluate against the corpus: log-scaled count terms; saturation set from corpus percentiles; counts normalized per kloc with a separate non-dilution guard; a smaller share for count terms. Any change bumps SCORING_VERSION and the test expectations together.\n\nAcceptance criteria\n1. The corpus record shows where each count term saturates across repo sizes and states whether a size-independent floor is intended.\n2. The chosen formula keeps the monotonicity rule and the no-dilution rule from SPEC 7.\n3. Repos of clearly different quality at similar size land at visibly different scores.","labels":["scoring","calibration"]}
{"id":"trellis-ff55","title":"Spike reusable quality providers on labeled controls and trellis","status":"closed","type":"task","priority":2,"createdAt":"2026-09-17T02:53:15.208Z","updatedAt":"2026-09-17T03:00:10.306Z","description":"Evaluate pinned jscpd, SonarJS, dependency-cruiser and Knip with repeatable offline runs after toolkit setup. Preserve provenance, scope and limitations; compare existing trellis metrics on controlled examples and its production source. Keep research outside audit/scoring and leave reproducible artifacts plus a synthesis.","closedAt":"2026-09-17T03:00:10.306Z"}
{"id":"trellis-06f8","title":"Design provider evidence integration from the reusable-analyzer spike","status":"open","type":"task","priority":2,"createdAt":"2026-09-17T02:57:46.879Z","updatedAt":"2026-09-17T02:57:46.879Z","description":"Follow up trellis-ff55. Define opt-in provider evidence with exact engine/parser/config/source identity, explicit scopes and missing-analysis states. Prioritize pinned jscpd adapter: line union, group semantics, malformed input, resource limits, platform packaging and repeated stability. Evaluate dependency-cruiser for declared architecture rules and Knip under explicit entry/test reachability. Resolve observed SonarJS package/header license discrepancy before any distribution decision. Keep outcome validation under trellis-f999; do not add uncalibrated counts to scoring."}
