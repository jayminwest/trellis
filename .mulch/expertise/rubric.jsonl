{"type":"decision","classification":"tactical","recorded_at":"2026-06-06T19:27:27.307Z","evidence":{"seeds":"trellis-2304","commit":"a535e057a3cc434036ff987d08180dfcb0255fe3"},"title":"Rubric core: schema/loader/version split + invariant placement","rationale":"trellis-2304 implements SPEC §6.1 rubric WHAT layer in src/rubric/ as three modules: schema.ts (zod per-record shape + cross-FIELD invariants), loader.ts (fs read + cross-RECORD invariants), version.ts (RUBRIC_VERSION + comparable()). Key decisions: (1) per-record invariants that need only the record live in zod via .superRefine — investigation non-null IFF discoveryVia=agent; weight via z.number().positive(); reserved gate/weight use .default(false)/.default(1) so YAML may omit them. (2) z.strictObject (not z.object) to reject typo'd keys; dropped .brand() to avoid zod v4 type friction with defaults+superRefine. (3) Cross-record invariants live in loader.ts because they need the whole set or the source file: scope-matches-file, unique ids (criteria deduped ACROSS both scope files via a shared Map), category refs resolve, exactly-one-gate-per-category. (4) RubricError(message,id,file) — every throw names offending id+file; schema failures map zod issues to a path:message string and recover the id best-effort via idOf(). (5) loadRubric(dir=import.meta.dir) reads the 3 YAML files; tests use temp dirs + fixtures (no fs mocks per convention) overriding a VALID baseline. (6) comparable() encodes SPEC §3.5: pre-1.0 comparability rides the MINOR slot so differing 0.x minors are NOT comparable; post-1.0 only major breaks. Data files (categories/repo-scope/app-scope.yaml) are authored separately in trellis-db3d — loader works against fixtures until then. Also un-ignored zod/js-yaml/@types/js-yaml in knip.json now that they're imported.","id":"mx-eb910a"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-06T19:31:43.327Z","evidence":{"seeds":"trellis-db3d","commit":"8ef7d6fab4b0ed3a05cd8cb1c47c68135e125a1b"},"relates_to":["mx-eb910a"],"title":"Rubric catalog data authored from SPEC §5 (90 criteria / 9 categories)","rationale":"trellis-db3d authors src/rubric/{categories,repo-scope,app-scope}.yaml as the WHAT layer (no tool names). Clean-room from SPEC §5 only, copying nothing from notes/PRIVATE-rubric. Layout decisions: criteria carry only schema fields (id/category/scope/level/skippable/discoveryVia/investigation, optional gate/weight) — the strictObject schema rejects extra keys, so the §5 'note' column lives in YAML comments, not data. gate/weight/null-investigation rely on schema defaults (omitted) matching the loader test fixture style. Grouped by category in §5 order within each scope file. Invariants verified by catalog.test.ts which runs the REAL loader against the REAL YAML (not temp dirs): 9 categories, 44 repo + 46 app = 90, 70 det / 20 agent, 9 gates one-per-category, every agent criterion names one of 4 areas (documentation/agent-config/setup-runnability/test-layout), every det criterion has null investigation. Gates: single_command_setup, type_check, unit_tests_runnable, deps_pinned, vcs_cli_tools, structured_logging, log_scrubbing, codeowners, import_cycle_detection.","id":"mx-b3aa7d"}
