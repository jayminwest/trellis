{"type":"decision","classification":"tactical","recorded_at":"2026-06-06T19:56:04.737Z","evidence":{"seeds":"trellis-a3b2","commit":"0b8060d5ed9566cf46b3404076812412fde69f05"},"title":"App discovery v0: deployable-marker walk, 0→root fallback, hint override","rationale":"trellis-a3b2 implements SPEC §8.2 in src/discovery/ as discover.ts (walk) + types.ts (App contract) + index barrel, pure given a filesystem. Key calls: (1) APP MARKER vs LANGUAGE are distinct per dir — a dir is an app only with a deployable marker (package.json w/ bin|main, pyproject.toml, Package.swift, Dockerfile=service dir); tsconfig.json & setup.cfg are LANGUAGE-ONLY signals, never app markers. Language labels are the detector Language union (ts/js→'typescript', no separate js). (2) EXCLUSION: walk skips node_modules/vendor/dist/build/out/coverage/target/__pycache__/venv AND any dotdir — so nested/vendored/built manifests never register. maxDepth guard default 8. (3) FALLBACK: 0 markers found → exactly 1 app at '.' with root-detected (or hinted) languages, so aggregateAppScope always has >=1 app (it throws on 0). (4) HINT: opts.languages (targets.yaml §6.5) OVERRIDES auto-detect for every app incl. fallback root; normalized via [...new Set].sort(). (5) DETERMINISM: final list sorted by path ('.' sorts first); readdir order irrelevant since output is sorted. (6) App carries {path,languages,description}; description = pkg.name ?? pkg.description ?? basename; toAppMap() projects to §6.3 {path:{description}}. App is a structural superset of detector ctx's {path,languages} so it flows straight into createDetectionContext. 16 tests (single-app, multi-app monorepo, mixed-lang, hint, exclusion, maxDepth, toAppMap); check:all exits 0.","id":"mx-192056"}
